(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('@protobuf-ts/grpcweb-transport'), require('sha.js'), require('ripemd160'), require('@scure/base'), require('@protobuf-ts/runtime-rpc'), require('@protobuf-ts/runtime'), require('bloom-filter'), require('@scure/btc-signer'), require('@noble/secp256k1'), require('@noble/curves/secp256k1.js'), require('@dashevo/x11-hash-js')) :
    typeof define === 'function' && define.amd ? define(['exports', '@protobuf-ts/grpcweb-transport', 'sha.js', 'ripemd160', '@scure/base', '@protobuf-ts/runtime-rpc', '@protobuf-ts/runtime', 'bloom-filter', '@scure/btc-signer', '@noble/secp256k1', '@noble/curves/secp256k1.js', '@dashevo/x11-hash-js'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.DashCoreSDK = {}, global.grpcwebTransport, global.sha_js, global.Ripemd160, global.base, global.runtimeRpc, global.runtime, global.BloomFilter$1, global.btcSigner, global.secp, global.secp256k1_js));
})(this, (function (exports, grpcwebTransport, sha_js, Ripemd160, base, runtimeRpc, runtime, BloomFilter$1, btcSigner, secp, secp256k1_js) { 'use strict';

    function _interopNamespaceDefault(e) {
        var n = Object.create(null);
        if (e) {
            Object.keys(e).forEach(function (k) {
                if (k !== 'default') {
                    var d = Object.getOwnPropertyDescriptor(e, k);
                    Object.defineProperty(n, k, d.get ? d : {
                        enumerable: true,
                        get: function () { return e[k]; }
                    });
                }
            });
        }
        n.default = e;
        return Object.freeze(n);
    }

    var secp__namespace = /*#__PURE__*/_interopNamespaceDefault(secp);

    async function getDAPINodeList(network) {
        const url = `https://${network === 'mainnet' ? '' : 'testnet.'}platform-explorer.pshenmic.dev/validators?isActive=true`;
        const resp = await fetch(url);
        if (resp.status !== 200) {
            throw new Error('Failed to query Platform Explorer for active validators');
        }
        const { resultSet } = await resp.json();
        return resultSet
            // eslint-disable-next-line
            .map((validator) => validator?.proTxInfo?.state?.service ? `https://${validator.proTxInfo.state.service.split(':')[0]}${network === 'mainnet' ? '' : ':1443'}` : undefined)
            .filter(e => e != null);
    }

    const DAPI_STREAM_RECONNECT_TIMEOUT = 250 * 1000;
    const DASH_VERSIONS = {
        testnet: { pubKeyHash: 0x8c, scriptHash: 0x13, bech32: 'dc', wif: 0xef, private: 0x04358394, public: 0x043587cf }
    };
    const FEE_PER_BYTE = 1;
    const MIN_FEE_RELAY = 1000;
    const SIGHASH_ALL = 0x01;
    /**
     * 32 - txId size
     * 4 - vout size
     * 8 - max scriptSigSize size
     * 108 - max scripSig size (SIG + PubKey Inner)
     * 4 - sequence
     */
    const SIGNED_INPUT_MAX_SIZE = 32 + 4 + 8 + 108 + 4;
    /**
     * 20 P2SH bytes
     * 4 OP_CODE bytes
     * 34 P2PK bytes
     * 4 varint/operators bytes
     * total: 62 bytes
     */
    const CHANGE_OUTPUT_MAX_SIZE = 20 + 4 + 34 + 4;
    const TRANSACTION_VERSION = 3;
    const DEFAULT_NLOCK_TIME = 0;
    /**
     * Before this value we must use block height as nLock value
     * On equal or greater this value, we must use unix timestamp
     * https://en.bitcoin.it/wiki/NLockTime
     */
    const NLOCK_TIME_BLOCK_BASED_LIMIT = 5e8;
    const OPCODES = {
        OP_0: 0,
        OP_PUSHBYTES_1: 1,
        OP_PUSHBYTES_2: 2,
        OP_PUSHBYTES_3: 3,
        OP_PUSHBYTES_4: 4,
        OP_PUSHBYTES_5: 5,
        OP_PUSHBYTES_6: 6,
        OP_PUSHBYTES_7: 7,
        OP_PUSHBYTES_8: 8,
        OP_PUSHBYTES_9: 9,
        OP_PUSHBYTES_10: 10,
        OP_PUSHBYTES_11: 11,
        OP_PUSHBYTES_12: 12,
        OP_PUSHBYTES_13: 13,
        OP_PUSHBYTES_14: 14,
        OP_PUSHBYTES_15: 15,
        OP_PUSHBYTES_16: 16,
        OP_PUSHBYTES_17: 17,
        OP_PUSHBYTES_18: 18,
        OP_PUSHBYTES_19: 19,
        OP_PUSHBYTES_20: 20,
        OP_PUSHBYTES_21: 21,
        OP_PUSHBYTES_22: 22,
        OP_PUSHBYTES_23: 23,
        OP_PUSHBYTES_24: 24,
        OP_PUSHBYTES_25: 25,
        OP_PUSHBYTES_26: 26,
        OP_PUSHBYTES_27: 27,
        OP_PUSHBYTES_28: 28,
        OP_PUSHBYTES_29: 29,
        OP_PUSHBYTES_30: 30,
        OP_PUSHBYTES_31: 31,
        OP_PUSHBYTES_32: 32,
        OP_PUSHBYTES_33: 33,
        OP_PUSHBYTES_34: 34,
        OP_PUSHBYTES_35: 35,
        OP_PUSHBYTES_36: 36,
        OP_PUSHBYTES_37: 37,
        OP_PUSHBYTES_38: 38,
        OP_PUSHBYTES_39: 39,
        OP_PUSHBYTES_40: 40,
        OP_PUSHBYTES_41: 41,
        OP_PUSHBYTES_42: 42,
        OP_PUSHBYTES_43: 43,
        OP_PUSHBYTES_44: 44,
        OP_PUSHBYTES_45: 45,
        OP_PUSHBYTES_46: 46,
        OP_PUSHBYTES_47: 47,
        OP_PUSHBYTES_48: 48,
        OP_PUSHBYTES_49: 49,
        OP_PUSHBYTES_50: 50,
        OP_PUSHBYTES_51: 51,
        OP_PUSHBYTES_52: 52,
        OP_PUSHBYTES_53: 53,
        OP_PUSHBYTES_54: 54,
        OP_PUSHBYTES_55: 55,
        OP_PUSHBYTES_56: 56,
        OP_PUSHBYTES_57: 57,
        OP_PUSHBYTES_58: 58,
        OP_PUSHBYTES_59: 59,
        OP_PUSHBYTES_60: 60,
        OP_PUSHBYTES_61: 61,
        OP_PUSHBYTES_62: 62,
        OP_PUSHBYTES_63: 63,
        OP_PUSHBYTES_64: 64,
        OP_PUSHBYTES_65: 65,
        OP_PUSHBYTES_66: 66,
        OP_PUSHBYTES_67: 67,
        OP_PUSHBYTES_68: 68,
        OP_PUSHBYTES_69: 69,
        OP_PUSHBYTES_70: 70,
        OP_PUSHBYTES_71: 71,
        OP_PUSHBYTES_72: 72,
        OP_PUSHBYTES_73: 73,
        OP_PUSHBYTES_74: 74,
        OP_PUSHBYTES_75: 75,
        OP_PUSHDATA1: 76,
        OP_PUSHDATA2: 77,
        OP_PUSHDATA4: 78,
        OP_1NEGATE: 79,
        OP_RESERVED: 80,
        OP_TRUE: 81,
        OP_1: 81,
        OP_2: 82,
        OP_3: 83,
        OP_4: 84,
        OP_5: 85,
        OP_6: 86,
        OP_7: 87,
        OP_8: 88,
        OP_9: 89,
        OP_10: 90,
        OP_11: 91,
        OP_12: 92,
        OP_13: 93,
        OP_14: 94,
        OP_15: 95,
        OP_16: 96,
        // control
        OP_NOP: 97,
        OP_VER: 98,
        OP_IF: 99,
        OP_NOTIF: 100,
        OP_VERIF: 101,
        OP_VERNOTIF: 102,
        OP_ELSE: 103,
        OP_ENDIF: 104,
        OP_VERIFY: 105,
        OP_RETURN: 106,
        // stack ops
        OP_TOALTSTACK: 107,
        OP_FROMALTSTACK: 108,
        OP_2DROP: 109,
        OP_2DUP: 110,
        OP_3DUP: 111,
        OP_2OVER: 112,
        OP_2ROT: 113,
        OP_2SWAP: 114,
        OP_IFDUP: 115,
        OP_DEPTH: 116,
        OP_DROP: 117,
        OP_DUP: 118,
        OP_NIP: 119,
        OP_OVER: 120,
        OP_PICK: 121,
        OP_ROLL: 122,
        OP_ROT: 123,
        OP_SWAP: 124,
        OP_TUCK: 125,
        // splice ops
        OP_CAT: 126,
        OP_SUBSTR: 127,
        OP_LEFT: 128,
        OP_RIGHT: 129,
        OP_SIZE: 130,
        // bit logic
        OP_INVERT: 131,
        OP_AND: 132,
        OP_OR: 133,
        OP_XOR: 134,
        OP_EQUAL: 135,
        OP_EQUALVERIFY: 136,
        OP_RESERVED1: 137,
        OP_RESERVED2: 138,
        // numeric
        OP_1ADD: 139,
        OP_1SUB: 140,
        OP_2MUL: 141,
        OP_2DIV: 142,
        OP_NEGATE: 143,
        OP_ABS: 144,
        OP_NOT: 145,
        OP_0NOTEQUAL: 146,
        OP_ADD: 147,
        OP_SUB: 148,
        OP_MUL: 149,
        OP_DIV: 150,
        OP_MOD: 151,
        OP_LSHIFT: 152,
        OP_RSHIFT: 153,
        OP_BOOLAND: 154,
        OP_BOOLOR: 155,
        OP_NUMEQUAL: 156,
        OP_NUMEQUALVERIFY: 157,
        OP_NUMNOTEQUAL: 158,
        OP_LESSTHAN: 159,
        OP_GREATERTHAN: 160,
        OP_LESSTHANOREQUAL: 161,
        OP_GREATERTHANOREQUAL: 162,
        OP_MIN: 163,
        OP_MAX: 164,
        OP_WITHIN: 165,
        // crypto
        OP_RIPEMD160: 166,
        OP_SHA1: 167,
        OP_SHA256: 168,
        OP_HASH160: 169,
        OP_HASH256: 170,
        OP_CODESEPARATOR: 171,
        OP_CHECKSIG: 172,
        OP_CHECKSIGVERIFY: 173,
        OP_CHECKMULTISIG: 174,
        OP_CHECKMULTISIGVERIFY: 175,
        OP_CHECKLOCKTIMEVERIFY: 177,
        OP_CHECKSEQUENCEVERIFY: 178,
        // expansion
        OP_NOP1: 176,
        OP_NOP2: 177,
        OP_NOP3: 178,
        OP_NOP4: 179,
        OP_NOP5: 180,
        OP_NOP6: 181,
        OP_NOP7: 182,
        OP_NOP8: 183,
        OP_NOP9: 184,
        OP_NOP10: 185,
        OP_CHECKSIGADD: 186,
        // synonyms for OP_RETURN
        OP_RETURN_187: 187,
        OP_RETURN_188: 188,
        OP_RETURN_189: 189,
        OP_RETURN_190: 190,
        OP_RETURN_191: 191,
        OP_RETURN_192: 192,
        OP_RETURN_193: 193,
        OP_RETURN_194: 194,
        OP_RETURN_195: 195,
        OP_RETURN_196: 196,
        OP_RETURN_197: 197,
        OP_RETURN_198: 198,
        OP_RETURN_199: 199,
        OP_RETURN_200: 200,
        OP_RETURN_201: 201,
        OP_RETURN_202: 202,
        OP_RETURN_203: 203,
        OP_RETURN_204: 204,
        OP_RETURN_205: 205,
        OP_RETURN_206: 206,
        OP_RETURN_207: 207,
        OP_RETURN_208: 208,
        OP_RETURN_209: 209,
        OP_RETURN_210: 210,
        OP_RETURN_211: 211,
        OP_RETURN_212: 212,
        OP_RETURN_213: 213,
        OP_RETURN_214: 214,
        OP_RETURN_215: 215,
        OP_RETURN_216: 216,
        OP_RETURN_217: 217,
        OP_RETURN_218: 218,
        OP_RETURN_219: 219,
        OP_RETURN_220: 220,
        OP_RETURN_221: 221,
        OP_RETURN_222: 222,
        OP_RETURN_223: 223,
        OP_RETURN_224: 224,
        OP_RETURN_225: 225,
        OP_RETURN_226: 226,
        OP_RETURN_227: 227,
        OP_RETURN_228: 228,
        OP_RETURN_229: 229,
        OP_RETURN_230: 230,
        OP_RETURN_231: 231,
        OP_RETURN_232: 232,
        OP_RETURN_233: 233,
        OP_RETURN_234: 234,
        OP_RETURN_235: 235,
        OP_RETURN_236: 236,
        OP_RETURN_237: 237,
        OP_RETURN_238: 238,
        OP_RETURN_239: 239,
        OP_RETURN_240: 240,
        OP_RETURN_241: 241,
        OP_RETURN_242: 242,
        OP_RETURN_243: 243,
        OP_RETURN_244: 244,
        OP_RETURN_245: 245,
        OP_RETURN_246: 246,
        OP_RETURN_247: 247,
        OP_RETURN_248: 248,
        OP_RETURN_249: 249,
        OP_RETURN_250: 250,
        OP_RETURN_251: 251,
        OP_RETURN_252: 252,
        OP_RETURN_253: 253,
        OP_RETURN_254: 254,
        OP_INVALIDOPCODE: 255
    };
    var Network;
    (function (Network) {
        Network[Network["Mainnet"] = 0] = "Mainnet";
        Network[Network["Testnet"] = 1] = "Testnet";
    })(Network || (Network = {}));
    /**
     * script network prefix:
     * mainnet: 16
     * testnet: 19
     *
     * for pubkey network prefix use this value:
     * mainnet: 76
     * testnet: 140
     */
    var NetworkPrefix;
    (function (NetworkPrefix) {
        NetworkPrefix[NetworkPrefix["ScriptPrefixTestnet"] = 19] = "ScriptPrefixTestnet";
        NetworkPrefix[NetworkPrefix["PubkeyPrefixTestnet"] = 140] = "PubkeyPrefixTestnet";
        NetworkPrefix[NetworkPrefix["ScriptPrefixMainnet"] = 16] = "ScriptPrefixMainnet";
        NetworkPrefix[NetworkPrefix["PubkeyPrefixMainnet"] = 76] = "PubkeyPrefixMainnet";
    })(NetworkPrefix || (NetworkPrefix = {}));
    const PubKeyHashAddressNetworkPrefix = {
        [Network.Mainnet]: NetworkPrefix.PubkeyPrefixMainnet,
        [Network.Testnet]: NetworkPrefix.PubkeyPrefixTestnet
    };
    const DEFAULT_NETWORK = Network.Testnet;
    const WIFNetworkPrefix = {
        204: Network.Mainnet,
        239: Network.Testnet,
        [Network.Mainnet]: 204,
        [Network.Testnet]: 239
    };
    const BLOOM_FILTER_FALSE_POSITIVE_RATE = 0.0001;

    const Base58Check = {
        encode: (data) => {
            const checksumHash = doubleSHA256(data);
            const out = new Uint8Array(data.byteLength + 4);
            out.set(data, 0);
            out.set(checksumHash.slice(0, 4), data.byteLength);
            return base.base58.encode(out);
        },
        decode: (data, skipVerify) => {
            const bytes = base.base58.decode(data);
            const body = bytes.slice(0, -4);
            if (skipVerify != null && skipVerify) {
                const passedChecksum = bytes.slice(-4);
                const correctedChecksum = doubleSHA256(body).slice(0, 4);
                if (bytesToHex(passedChecksum) !== bytesToHex(correctedChecksum)) {
                    throw new Error('Base58check decode failed: incorrect checksum provided');
                }
            }
            return body;
        }
    };

    function getRandomArrayItem(array) {
        return array[Math.floor((Math.random() * array.length))];
    }
    function hexToBytes(hex) {
        return Uint8Array.from((hex.match(/.{1,2}/g) ?? []).map((byte) => parseInt(byte, 16)));
    }
    function bytesToHex(bytes) {
        return Array.prototype.map.call(bytes, (x) => ('00' + x.toString(16)).slice(-2)).join('');
    }
    function decodeCompactSize(offset, bytes) {
        const dataView = new DataView(bytes.buffer);
        const prefix = dataView.getUint8(offset);
        if (prefix <= 252) {
            return prefix;
        }
        else if (prefix === 253) {
            return dataView.getUint16(offset + 1, true);
        }
        else if (prefix === 254) {
            return dataView.getUint32(offset + 1, true);
        }
        else {
            return dataView.getBigUint64(offset + 1, true);
        }
    }
    function encodeCompactSize(num) {
        if (num <= 252) {
            return new Uint8Array([Number(num)]);
        }
        else if (num > 252 && num <= 65535) {
            const dataView = new DataView(new ArrayBuffer(2));
            dataView.setUint16(0, Number(num), true);
            const out = new Uint8Array(3);
            out.set(new Uint8Array([253]), 0);
            out.set(new Uint8Array(dataView.buffer), 1);
            return out;
        }
        else if (num > 65535 && num <= 4294967295) {
            const dataView = new DataView(new ArrayBuffer(4));
            dataView.setUint32(0, Number(num), true);
            const out = new Uint8Array(5);
            out.set(new Uint8Array([254]), 0);
            out.set(new Uint8Array(dataView.buffer), 1);
            return out;
        }
        else if (BigInt(num) > BigInt(4294967295) && BigInt(num) <= BigInt('18446744073709551615')) {
            const dataView = new DataView(new ArrayBuffer(8));
            dataView.setBigUint64(0, BigInt(num), true);
            const out = new Uint8Array(9);
            out.set(new Uint8Array([255]), 0);
            out.set(new Uint8Array(dataView.buffer), 1);
            return out;
        }
        else {
            throw new Error('Cannot encode compactSize number');
        }
    }
    function getCompactVariableSize(num) {
        if (num <= 252) {
            return 1;
        }
        else if (num > 252 && num <= 65535) {
            return 3;
        }
        else if (num > 65535 && num <= 4294967295) {
            return 5;
        }
        else {
            return 9;
        }
    }
    async function wait(ms) {
        return await new Promise(resolve => setTimeout(resolve, ms));
    }
    function doubleSHA256(data) {
        const firstStage = new Uint8Array(new sha_js.sha256().update(data).digest());
        return new Uint8Array(new sha_js.sha256().update(firstStage).digest());
    }
    function SHA256RIPEMD160(data) {
        const firstStage = new Uint8Array(new sha_js.sha256().update(data).digest());
        return new Uint8Array(new Ripemd160().update(firstStage).digest());
    }
    function networkValueToEnumValue(value) {
        if (typeof value === 'string') {
            if (value.toLowerCase() === 'mainnet') {
                return Network.Mainnet;
            }
            else {
                return Network.Testnet;
            }
        }
        else {
            return value;
        }
    }
    function addressToPublicKeyHash(address) {
        return Base58Check.decode(address).slice(1);
    }
    function publicKeyHashToAddress(hash, network = DEFAULT_NETWORK) {
        const normalNetwork = networkValueToEnumValue(network);
        const prefix = PubKeyHashAddressNetworkPrefix[normalNetwork];
        const address = new Uint8Array(1 + hash.byteLength);
        address.set([prefix], 0);
        address.set(hash, 1);
        return Base58Check.encode(address);
    }
    function bytesToIp(bytes) {
        const canContainIPv6 = bytes.byteLength > 4;
        if (canContainIPv6) {
            const isIPv4Mapped = bytes.slice(0, 10).every(b => b === 0) &&
                bytes[10] === 0xff &&
                bytes[11] === 0xff;
            if (isIPv4Mapped) {
                return Array.from(bytes.slice(12)).join('.');
            }
            else {
                const parts = [];
                const view = new DataView(bytes.buffer);
                for (let i = 0; i < bytes.length; i += 2) {
                    parts.push(view.getUint16(i, false).toString(16));
                }
                return parts
                    .map(part => part.padStart(4, '0'))
                    .join(':');
            }
        }
        else if (bytes.byteLength === 4) {
            return Array.from(bytes).join('.');
        }
        throw new Error('Could not parse IPv6 or IPv4 address');
    }
    function ipToBytes(ip, compact) {
        if (ip.includes('.') && !ip.includes(':')) {
            const parts = ip.split('.');
            if (parts.length !== 4)
                throw new Error('Invalid IPv4 address');
            const ipBytes = new Uint8Array(parts.map(part => {
                const num = Number(part);
                if (isNaN(num) || num < 0 || num > 255)
                    throw new Error('Invalid IPv4 byte');
                return num;
            }));
            const fullBytes = new Uint8Array(16);
            const flagBytes = new Uint8Array([0xff, 0xff]);
            fullBytes.set([...ipBytes.toReversed(), ...flagBytes], 0);
            return fullBytes.toReversed();
        }
        if (ip.includes(':')) {
            const parts = ip.split(':');
            if (parts.length !== 8)
                throw new Error('Invalid IPv6 address');
            const bytes = new Uint8Array(16);
            const view = new DataView(bytes.buffer);
            for (let i = 0; i < parts.length; i += 1) {
                const val = Number(`0x${parts[i]}`);
                if (isNaN(val) || val < 0 || val > 0xffff)
                    throw new Error('Invalid IPv6 part');
                view.setUint16(i * 2, val, false);
            }
            return bytes;
        }
        throw new Error('Invalid IP address format');
    }

    // @generated by protobuf-ts 2.11.1 with parameter generate_dependencies,client_generic
    // @generated from protobuf file "core.proto" (package "org.dash.platform.dapi.v0", syntax proto3)
    // tslint:disable
    /**
     * @generated from protobuf enum org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Status
     */
    var GetBlockchainStatusResponse_Status;
    (function (GetBlockchainStatusResponse_Status) {
        /**
         * @generated from protobuf enum value: NOT_STARTED = 0;
         */
        GetBlockchainStatusResponse_Status[GetBlockchainStatusResponse_Status["NOT_STARTED"] = 0] = "NOT_STARTED";
        /**
         * @generated from protobuf enum value: SYNCING = 1;
         */
        GetBlockchainStatusResponse_Status[GetBlockchainStatusResponse_Status["SYNCING"] = 1] = "SYNCING";
        /**
         * @generated from protobuf enum value: READY = 2;
         */
        GetBlockchainStatusResponse_Status[GetBlockchainStatusResponse_Status["READY"] = 2] = "READY";
        /**
         * @generated from protobuf enum value: ERROR = 3;
         */
        GetBlockchainStatusResponse_Status[GetBlockchainStatusResponse_Status["ERROR"] = 3] = "ERROR";
    })(GetBlockchainStatusResponse_Status || (GetBlockchainStatusResponse_Status = {}));
    /**
     * @generated from protobuf enum org.dash.platform.dapi.v0.GetMasternodeStatusResponse.Status
     */
    var GetMasternodeStatusResponse_Status;
    (function (GetMasternodeStatusResponse_Status) {
        /**
         * @generated from protobuf enum value: UNKNOWN = 0;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["UNKNOWN"] = 0] = "UNKNOWN";
        /**
         * @generated from protobuf enum value: WAITING_FOR_PROTX = 1;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["WAITING_FOR_PROTX"] = 1] = "WAITING_FOR_PROTX";
        /**
         * @generated from protobuf enum value: POSE_BANNED = 2;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["POSE_BANNED"] = 2] = "POSE_BANNED";
        /**
         * @generated from protobuf enum value: REMOVED = 3;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["REMOVED"] = 3] = "REMOVED";
        /**
         * @generated from protobuf enum value: OPERATOR_KEY_CHANGED = 4;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["OPERATOR_KEY_CHANGED"] = 4] = "OPERATOR_KEY_CHANGED";
        /**
         * @generated from protobuf enum value: PROTX_IP_CHANGED = 5;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["PROTX_IP_CHANGED"] = 5] = "PROTX_IP_CHANGED";
        /**
         * @generated from protobuf enum value: READY = 6;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["READY"] = 6] = "READY";
        /**
         * @generated from protobuf enum value: ERROR = 7;
         */
        GetMasternodeStatusResponse_Status[GetMasternodeStatusResponse_Status["ERROR"] = 7] = "ERROR";
    })(GetMasternodeStatusResponse_Status || (GetMasternodeStatusResponse_Status = {}));
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusRequest", []);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusRequest
     */
    const GetBlockchainStatusRequest = new GetBlockchainStatusRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusResponse", [
                { no: 1, name: "version", kind: "message", T: () => GetBlockchainStatusResponse_Version },
                { no: 2, name: "time", kind: "message", T: () => GetBlockchainStatusResponse_Time },
                { no: 3, name: "status", kind: "enum", T: () => ["org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Status", GetBlockchainStatusResponse_Status] },
                { no: 4, name: "sync_progress", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ },
                { no: 5, name: "chain", kind: "message", T: () => GetBlockchainStatusResponse_Chain },
                { no: 7, name: "network", kind: "message", T: () => GetBlockchainStatusResponse_Network }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.status = 0;
            message.syncProgress = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Version version */ 1:
                        message.version = GetBlockchainStatusResponse_Version.internalBinaryRead(reader, reader.uint32(), options, message.version);
                        break;
                    case /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Time time */ 2:
                        message.time = GetBlockchainStatusResponse_Time.internalBinaryRead(reader, reader.uint32(), options, message.time);
                        break;
                    case /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Status status */ 3:
                        message.status = reader.int32();
                        break;
                    case /* double sync_progress */ 4:
                        message.syncProgress = reader.double();
                        break;
                    case /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Chain chain */ 5:
                        message.chain = GetBlockchainStatusResponse_Chain.internalBinaryRead(reader, reader.uint32(), options, message.chain);
                        break;
                    case /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Network network */ 7:
                        message.network = GetBlockchainStatusResponse_Network.internalBinaryRead(reader, reader.uint32(), options, message.network);
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Version version = 1; */
            if (message.version)
                GetBlockchainStatusResponse_Version.internalBinaryWrite(message.version, writer.tag(1, runtime.WireType.LengthDelimited).fork(), options).join();
            /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Time time = 2; */
            if (message.time)
                GetBlockchainStatusResponse_Time.internalBinaryWrite(message.time, writer.tag(2, runtime.WireType.LengthDelimited).fork(), options).join();
            /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Status status = 3; */
            if (message.status !== 0)
                writer.tag(3, runtime.WireType.Varint).int32(message.status);
            /* double sync_progress = 4; */
            if (message.syncProgress !== 0)
                writer.tag(4, runtime.WireType.Bit64).double(message.syncProgress);
            /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Chain chain = 5; */
            if (message.chain)
                GetBlockchainStatusResponse_Chain.internalBinaryWrite(message.chain, writer.tag(5, runtime.WireType.LengthDelimited).fork(), options).join();
            /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Network network = 7; */
            if (message.network)
                GetBlockchainStatusResponse_Network.internalBinaryWrite(message.network, writer.tag(7, runtime.WireType.LengthDelimited).fork(), options).join();
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusResponse
     */
    const GetBlockchainStatusResponse = new GetBlockchainStatusResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusResponse_Version$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Version", [
                { no: 1, name: "protocol", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 2, name: "software", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 3, name: "agent", kind: "scalar", T: 9 /*ScalarType.STRING*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.protocol = 0;
            message.software = 0;
            message.agent = "";
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* uint32 protocol */ 1:
                        message.protocol = reader.uint32();
                        break;
                    case /* uint32 software */ 2:
                        message.software = reader.uint32();
                        break;
                    case /* string agent */ 3:
                        message.agent = reader.string();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* uint32 protocol = 1; */
            if (message.protocol !== 0)
                writer.tag(1, runtime.WireType.Varint).uint32(message.protocol);
            /* uint32 software = 2; */
            if (message.software !== 0)
                writer.tag(2, runtime.WireType.Varint).uint32(message.software);
            /* string agent = 3; */
            if (message.agent !== "")
                writer.tag(3, runtime.WireType.LengthDelimited).string(message.agent);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Version
     */
    const GetBlockchainStatusResponse_Version = new GetBlockchainStatusResponse_Version$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusResponse_Time$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Time", [
                { no: 1, name: "now", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 2, name: "offset", kind: "scalar", T: 5 /*ScalarType.INT32*/ },
                { no: 3, name: "median", kind: "scalar", T: 13 /*ScalarType.UINT32*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.now = 0;
            message.offset = 0;
            message.median = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* uint32 now */ 1:
                        message.now = reader.uint32();
                        break;
                    case /* int32 offset */ 2:
                        message.offset = reader.int32();
                        break;
                    case /* uint32 median */ 3:
                        message.median = reader.uint32();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* uint32 now = 1; */
            if (message.now !== 0)
                writer.tag(1, runtime.WireType.Varint).uint32(message.now);
            /* int32 offset = 2; */
            if (message.offset !== 0)
                writer.tag(2, runtime.WireType.Varint).int32(message.offset);
            /* uint32 median = 3; */
            if (message.median !== 0)
                writer.tag(3, runtime.WireType.Varint).uint32(message.median);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Time
     */
    const GetBlockchainStatusResponse_Time = new GetBlockchainStatusResponse_Time$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusResponse_Chain$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Chain", [
                { no: 1, name: "name", kind: "scalar", T: 9 /*ScalarType.STRING*/ },
                { no: 2, name: "headers_count", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 3, name: "blocks_count", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 4, name: "best_block_hash", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 5, name: "difficulty", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ },
                { no: 6, name: "chain_work", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 7, name: "is_synced", kind: "scalar", T: 8 /*ScalarType.BOOL*/ },
                { no: 8, name: "sync_progress", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.name = "";
            message.headersCount = 0;
            message.blocksCount = 0;
            message.bestBlockHash = new Uint8Array(0);
            message.difficulty = 0;
            message.chainWork = new Uint8Array(0);
            message.isSynced = false;
            message.syncProgress = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* string name */ 1:
                        message.name = reader.string();
                        break;
                    case /* uint32 headers_count */ 2:
                        message.headersCount = reader.uint32();
                        break;
                    case /* uint32 blocks_count */ 3:
                        message.blocksCount = reader.uint32();
                        break;
                    case /* bytes best_block_hash */ 4:
                        message.bestBlockHash = reader.bytes();
                        break;
                    case /* double difficulty */ 5:
                        message.difficulty = reader.double();
                        break;
                    case /* bytes chain_work */ 6:
                        message.chainWork = reader.bytes();
                        break;
                    case /* bool is_synced */ 7:
                        message.isSynced = reader.bool();
                        break;
                    case /* double sync_progress */ 8:
                        message.syncProgress = reader.double();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* string name = 1; */
            if (message.name !== "")
                writer.tag(1, runtime.WireType.LengthDelimited).string(message.name);
            /* uint32 headers_count = 2; */
            if (message.headersCount !== 0)
                writer.tag(2, runtime.WireType.Varint).uint32(message.headersCount);
            /* uint32 blocks_count = 3; */
            if (message.blocksCount !== 0)
                writer.tag(3, runtime.WireType.Varint).uint32(message.blocksCount);
            /* bytes best_block_hash = 4; */
            if (message.bestBlockHash.length)
                writer.tag(4, runtime.WireType.LengthDelimited).bytes(message.bestBlockHash);
            /* double difficulty = 5; */
            if (message.difficulty !== 0)
                writer.tag(5, runtime.WireType.Bit64).double(message.difficulty);
            /* bytes chain_work = 6; */
            if (message.chainWork.length)
                writer.tag(6, runtime.WireType.LengthDelimited).bytes(message.chainWork);
            /* bool is_synced = 7; */
            if (message.isSynced !== false)
                writer.tag(7, runtime.WireType.Varint).bool(message.isSynced);
            /* double sync_progress = 8; */
            if (message.syncProgress !== 0)
                writer.tag(8, runtime.WireType.Bit64).double(message.syncProgress);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Chain
     */
    const GetBlockchainStatusResponse_Chain = new GetBlockchainStatusResponse_Chain$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusResponse_NetworkFee$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusResponse.NetworkFee", [
                { no: 1, name: "relay", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ },
                { no: 2, name: "incremental", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.relay = 0;
            message.incremental = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* double relay */ 1:
                        message.relay = reader.double();
                        break;
                    case /* double incremental */ 2:
                        message.incremental = reader.double();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* double relay = 1; */
            if (message.relay !== 0)
                writer.tag(1, runtime.WireType.Bit64).double(message.relay);
            /* double incremental = 2; */
            if (message.incremental !== 0)
                writer.tag(2, runtime.WireType.Bit64).double(message.incremental);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusResponse.NetworkFee
     */
    const GetBlockchainStatusResponse_NetworkFee = new GetBlockchainStatusResponse_NetworkFee$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockchainStatusResponse_Network$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Network", [
                { no: 1, name: "peers_count", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 2, name: "fee", kind: "message", T: () => GetBlockchainStatusResponse_NetworkFee }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.peersCount = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* uint32 peers_count */ 1:
                        message.peersCount = reader.uint32();
                        break;
                    case /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.NetworkFee fee */ 2:
                        message.fee = GetBlockchainStatusResponse_NetworkFee.internalBinaryRead(reader, reader.uint32(), options, message.fee);
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* uint32 peers_count = 1; */
            if (message.peersCount !== 0)
                writer.tag(1, runtime.WireType.Varint).uint32(message.peersCount);
            /* org.dash.platform.dapi.v0.GetBlockchainStatusResponse.NetworkFee fee = 2; */
            if (message.fee)
                GetBlockchainStatusResponse_NetworkFee.internalBinaryWrite(message.fee, writer.tag(2, runtime.WireType.LengthDelimited).fork(), options).join();
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockchainStatusResponse.Network
     */
    const GetBlockchainStatusResponse_Network = new GetBlockchainStatusResponse_Network$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetMasternodeStatusRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetMasternodeStatusRequest", []);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetMasternodeStatusRequest
     */
    const GetMasternodeStatusRequest = new GetMasternodeStatusRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetMasternodeStatusResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetMasternodeStatusResponse", [
                { no: 1, name: "status", kind: "enum", T: () => ["org.dash.platform.dapi.v0.GetMasternodeStatusResponse.Status", GetMasternodeStatusResponse_Status] },
                { no: 2, name: "pro_tx_hash", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 3, name: "pose_penalty", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 4, name: "is_synced", kind: "scalar", T: 8 /*ScalarType.BOOL*/ },
                { no: 5, name: "sync_progress", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.status = 0;
            message.proTxHash = new Uint8Array(0);
            message.posePenalty = 0;
            message.isSynced = false;
            message.syncProgress = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* org.dash.platform.dapi.v0.GetMasternodeStatusResponse.Status status */ 1:
                        message.status = reader.int32();
                        break;
                    case /* bytes pro_tx_hash */ 2:
                        message.proTxHash = reader.bytes();
                        break;
                    case /* uint32 pose_penalty */ 3:
                        message.posePenalty = reader.uint32();
                        break;
                    case /* bool is_synced */ 4:
                        message.isSynced = reader.bool();
                        break;
                    case /* double sync_progress */ 5:
                        message.syncProgress = reader.double();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* org.dash.platform.dapi.v0.GetMasternodeStatusResponse.Status status = 1; */
            if (message.status !== 0)
                writer.tag(1, runtime.WireType.Varint).int32(message.status);
            /* bytes pro_tx_hash = 2; */
            if (message.proTxHash.length)
                writer.tag(2, runtime.WireType.LengthDelimited).bytes(message.proTxHash);
            /* uint32 pose_penalty = 3; */
            if (message.posePenalty !== 0)
                writer.tag(3, runtime.WireType.Varint).uint32(message.posePenalty);
            /* bool is_synced = 4; */
            if (message.isSynced !== false)
                writer.tag(4, runtime.WireType.Varint).bool(message.isSynced);
            /* double sync_progress = 5; */
            if (message.syncProgress !== 0)
                writer.tag(5, runtime.WireType.Bit64).double(message.syncProgress);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetMasternodeStatusResponse
     */
    const GetMasternodeStatusResponse = new GetMasternodeStatusResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockRequest", [
                { no: 1, name: "height", kind: "scalar", oneof: "block", T: 13 /*ScalarType.UINT32*/ },
                { no: 2, name: "hash", kind: "scalar", oneof: "block", T: 9 /*ScalarType.STRING*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.block = { oneofKind: undefined };
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* uint32 height */ 1:
                        message.block = {
                            oneofKind: "height",
                            height: reader.uint32()
                        };
                        break;
                    case /* string hash */ 2:
                        message.block = {
                            oneofKind: "hash",
                            hash: reader.string()
                        };
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* uint32 height = 1; */
            if (message.block.oneofKind === "height")
                writer.tag(1, runtime.WireType.Varint).uint32(message.block.height);
            /* string hash = 2; */
            if (message.block.oneofKind === "hash")
                writer.tag(2, runtime.WireType.LengthDelimited).string(message.block.hash);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockRequest
     */
    const GetBlockRequest = new GetBlockRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBlockResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBlockResponse", [
                { no: 1, name: "block", kind: "scalar", T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.block = new Uint8Array(0);
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* bytes block */ 1:
                        message.block = reader.bytes();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* bytes block = 1; */
            if (message.block.length)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.block);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBlockResponse
     */
    const GetBlockResponse = new GetBlockResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBestBlockHeightRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBestBlockHeightRequest", []);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBestBlockHeightRequest
     */
    const GetBestBlockHeightRequest = new GetBestBlockHeightRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetBestBlockHeightResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetBestBlockHeightResponse", [
                { no: 1, name: "height", kind: "scalar", T: 13 /*ScalarType.UINT32*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.height = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* uint32 height */ 1:
                        message.height = reader.uint32();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* uint32 height = 1; */
            if (message.height !== 0)
                writer.tag(1, runtime.WireType.Varint).uint32(message.height);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetBestBlockHeightResponse
     */
    const GetBestBlockHeightResponse = new GetBestBlockHeightResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class BroadcastTransactionRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.BroadcastTransactionRequest", [
                { no: 1, name: "transaction", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 2, name: "allow_high_fees", kind: "scalar", T: 8 /*ScalarType.BOOL*/ },
                { no: 3, name: "bypass_limits", kind: "scalar", T: 8 /*ScalarType.BOOL*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.transaction = new Uint8Array(0);
            message.allowHighFees = false;
            message.bypassLimits = false;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* bytes transaction */ 1:
                        message.transaction = reader.bytes();
                        break;
                    case /* bool allow_high_fees */ 2:
                        message.allowHighFees = reader.bool();
                        break;
                    case /* bool bypass_limits */ 3:
                        message.bypassLimits = reader.bool();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* bytes transaction = 1; */
            if (message.transaction.length)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.transaction);
            /* bool allow_high_fees = 2; */
            if (message.allowHighFees !== false)
                writer.tag(2, runtime.WireType.Varint).bool(message.allowHighFees);
            /* bool bypass_limits = 3; */
            if (message.bypassLimits !== false)
                writer.tag(3, runtime.WireType.Varint).bool(message.bypassLimits);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.BroadcastTransactionRequest
     */
    const BroadcastTransactionRequest = new BroadcastTransactionRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class BroadcastTransactionResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.BroadcastTransactionResponse", [
                { no: 1, name: "transaction_id", kind: "scalar", T: 9 /*ScalarType.STRING*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.transactionId = "";
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* string transaction_id */ 1:
                        message.transactionId = reader.string();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* string transaction_id = 1; */
            if (message.transactionId !== "")
                writer.tag(1, runtime.WireType.LengthDelimited).string(message.transactionId);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.BroadcastTransactionResponse
     */
    const BroadcastTransactionResponse = new BroadcastTransactionResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetTransactionRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetTransactionRequest", [
                { no: 1, name: "id", kind: "scalar", T: 9 /*ScalarType.STRING*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.id = "";
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* string id */ 1:
                        message.id = reader.string();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* string id = 1; */
            if (message.id !== "")
                writer.tag(1, runtime.WireType.LengthDelimited).string(message.id);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetTransactionRequest
     */
    const GetTransactionRequest = new GetTransactionRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetTransactionResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetTransactionResponse", [
                { no: 1, name: "transaction", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 2, name: "block_hash", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 3, name: "height", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 4, name: "confirmations", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 5, name: "is_instant_locked", kind: "scalar", T: 8 /*ScalarType.BOOL*/ },
                { no: 6, name: "is_chain_locked", kind: "scalar", T: 8 /*ScalarType.BOOL*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.transaction = new Uint8Array(0);
            message.blockHash = new Uint8Array(0);
            message.height = 0;
            message.confirmations = 0;
            message.isInstantLocked = false;
            message.isChainLocked = false;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* bytes transaction */ 1:
                        message.transaction = reader.bytes();
                        break;
                    case /* bytes block_hash */ 2:
                        message.blockHash = reader.bytes();
                        break;
                    case /* uint32 height */ 3:
                        message.height = reader.uint32();
                        break;
                    case /* uint32 confirmations */ 4:
                        message.confirmations = reader.uint32();
                        break;
                    case /* bool is_instant_locked */ 5:
                        message.isInstantLocked = reader.bool();
                        break;
                    case /* bool is_chain_locked */ 6:
                        message.isChainLocked = reader.bool();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* bytes transaction = 1; */
            if (message.transaction.length)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.transaction);
            /* bytes block_hash = 2; */
            if (message.blockHash.length)
                writer.tag(2, runtime.WireType.LengthDelimited).bytes(message.blockHash);
            /* uint32 height = 3; */
            if (message.height !== 0)
                writer.tag(3, runtime.WireType.Varint).uint32(message.height);
            /* uint32 confirmations = 4; */
            if (message.confirmations !== 0)
                writer.tag(4, runtime.WireType.Varint).uint32(message.confirmations);
            /* bool is_instant_locked = 5; */
            if (message.isInstantLocked !== false)
                writer.tag(5, runtime.WireType.Varint).bool(message.isInstantLocked);
            /* bool is_chain_locked = 6; */
            if (message.isChainLocked !== false)
                writer.tag(6, runtime.WireType.Varint).bool(message.isChainLocked);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetTransactionResponse
     */
    const GetTransactionResponse = new GetTransactionResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class BlockHeadersWithChainLocksRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.BlockHeadersWithChainLocksRequest", [
                { no: 1, name: "from_block_hash", kind: "scalar", oneof: "fromBlock", T: 12 /*ScalarType.BYTES*/ },
                { no: 2, name: "from_block_height", kind: "scalar", oneof: "fromBlock", T: 13 /*ScalarType.UINT32*/ },
                { no: 3, name: "count", kind: "scalar", T: 13 /*ScalarType.UINT32*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.fromBlock = { oneofKind: undefined };
            message.count = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* bytes from_block_hash */ 1:
                        message.fromBlock = {
                            oneofKind: "fromBlockHash",
                            fromBlockHash: reader.bytes()
                        };
                        break;
                    case /* uint32 from_block_height */ 2:
                        message.fromBlock = {
                            oneofKind: "fromBlockHeight",
                            fromBlockHeight: reader.uint32()
                        };
                        break;
                    case /* uint32 count */ 3:
                        message.count = reader.uint32();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* bytes from_block_hash = 1; */
            if (message.fromBlock.oneofKind === "fromBlockHash")
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.fromBlock.fromBlockHash);
            /* uint32 from_block_height = 2; */
            if (message.fromBlock.oneofKind === "fromBlockHeight")
                writer.tag(2, runtime.WireType.Varint).uint32(message.fromBlock.fromBlockHeight);
            /* uint32 count = 3; */
            if (message.count !== 0)
                writer.tag(3, runtime.WireType.Varint).uint32(message.count);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.BlockHeadersWithChainLocksRequest
     */
    const BlockHeadersWithChainLocksRequest = new BlockHeadersWithChainLocksRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class BlockHeadersWithChainLocksResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.BlockHeadersWithChainLocksResponse", [
                { no: 1, name: "block_headers", kind: "message", oneof: "responses", T: () => BlockHeaders },
                { no: 2, name: "chain_lock", kind: "scalar", oneof: "responses", T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.responses = { oneofKind: undefined };
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* org.dash.platform.dapi.v0.BlockHeaders block_headers */ 1:
                        message.responses = {
                            oneofKind: "blockHeaders",
                            blockHeaders: BlockHeaders.internalBinaryRead(reader, reader.uint32(), options, message.responses.blockHeaders)
                        };
                        break;
                    case /* bytes chain_lock */ 2:
                        message.responses = {
                            oneofKind: "chainLock",
                            chainLock: reader.bytes()
                        };
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* org.dash.platform.dapi.v0.BlockHeaders block_headers = 1; */
            if (message.responses.oneofKind === "blockHeaders")
                BlockHeaders.internalBinaryWrite(message.responses.blockHeaders, writer.tag(1, runtime.WireType.LengthDelimited).fork(), options).join();
            /* bytes chain_lock = 2; */
            if (message.responses.oneofKind === "chainLock")
                writer.tag(2, runtime.WireType.LengthDelimited).bytes(message.responses.chainLock);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.BlockHeadersWithChainLocksResponse
     */
    const BlockHeadersWithChainLocksResponse = new BlockHeadersWithChainLocksResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class BlockHeaders$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.BlockHeaders", [
                { no: 1, name: "headers", kind: "scalar", repeat: 2 /*RepeatType.UNPACKED*/, T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.headers = [];
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* repeated bytes headers */ 1:
                        message.headers.push(reader.bytes());
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* repeated bytes headers = 1; */
            for (let i = 0; i < message.headers.length; i++)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.headers[i]);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.BlockHeaders
     */
    const BlockHeaders = new BlockHeaders$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetEstimatedTransactionFeeRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetEstimatedTransactionFeeRequest", [
                { no: 1, name: "blocks", kind: "scalar", T: 13 /*ScalarType.UINT32*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.blocks = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* uint32 blocks */ 1:
                        message.blocks = reader.uint32();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* uint32 blocks = 1; */
            if (message.blocks !== 0)
                writer.tag(1, runtime.WireType.Varint).uint32(message.blocks);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetEstimatedTransactionFeeRequest
     */
    const GetEstimatedTransactionFeeRequest = new GetEstimatedTransactionFeeRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class GetEstimatedTransactionFeeResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.GetEstimatedTransactionFeeResponse", [
                { no: 1, name: "fee", kind: "scalar", T: 1 /*ScalarType.DOUBLE*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.fee = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* double fee */ 1:
                        message.fee = reader.double();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* double fee = 1; */
            if (message.fee !== 0)
                writer.tag(1, runtime.WireType.Bit64).double(message.fee);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.GetEstimatedTransactionFeeResponse
     */
    const GetEstimatedTransactionFeeResponse = new GetEstimatedTransactionFeeResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class TransactionsWithProofsRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.TransactionsWithProofsRequest", [
                { no: 1, name: "bloom_filter", kind: "message", T: () => BloomFilter },
                { no: 2, name: "from_block_hash", kind: "scalar", oneof: "fromBlock", T: 12 /*ScalarType.BYTES*/ },
                { no: 3, name: "from_block_height", kind: "scalar", oneof: "fromBlock", T: 13 /*ScalarType.UINT32*/ },
                { no: 4, name: "count", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 5, name: "send_transaction_hashes", kind: "scalar", T: 8 /*ScalarType.BOOL*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.fromBlock = { oneofKind: undefined };
            message.count = 0;
            message.sendTransactionHashes = false;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* org.dash.platform.dapi.v0.BloomFilter bloom_filter */ 1:
                        message.bloomFilter = BloomFilter.internalBinaryRead(reader, reader.uint32(), options, message.bloomFilter);
                        break;
                    case /* bytes from_block_hash */ 2:
                        message.fromBlock = {
                            oneofKind: "fromBlockHash",
                            fromBlockHash: reader.bytes()
                        };
                        break;
                    case /* uint32 from_block_height */ 3:
                        message.fromBlock = {
                            oneofKind: "fromBlockHeight",
                            fromBlockHeight: reader.uint32()
                        };
                        break;
                    case /* uint32 count */ 4:
                        message.count = reader.uint32();
                        break;
                    case /* bool send_transaction_hashes */ 5:
                        message.sendTransactionHashes = reader.bool();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* org.dash.platform.dapi.v0.BloomFilter bloom_filter = 1; */
            if (message.bloomFilter)
                BloomFilter.internalBinaryWrite(message.bloomFilter, writer.tag(1, runtime.WireType.LengthDelimited).fork(), options).join();
            /* bytes from_block_hash = 2; */
            if (message.fromBlock.oneofKind === "fromBlockHash")
                writer.tag(2, runtime.WireType.LengthDelimited).bytes(message.fromBlock.fromBlockHash);
            /* uint32 from_block_height = 3; */
            if (message.fromBlock.oneofKind === "fromBlockHeight")
                writer.tag(3, runtime.WireType.Varint).uint32(message.fromBlock.fromBlockHeight);
            /* uint32 count = 4; */
            if (message.count !== 0)
                writer.tag(4, runtime.WireType.Varint).uint32(message.count);
            /* bool send_transaction_hashes = 5; */
            if (message.sendTransactionHashes !== false)
                writer.tag(5, runtime.WireType.Varint).bool(message.sendTransactionHashes);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.TransactionsWithProofsRequest
     */
    const TransactionsWithProofsRequest = new TransactionsWithProofsRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class BloomFilter$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.BloomFilter", [
                { no: 1, name: "v_data", kind: "scalar", T: 12 /*ScalarType.BYTES*/ },
                { no: 2, name: "n_hash_funcs", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 3, name: "n_tweak", kind: "scalar", T: 13 /*ScalarType.UINT32*/ },
                { no: 4, name: "n_flags", kind: "scalar", T: 13 /*ScalarType.UINT32*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.vData = new Uint8Array(0);
            message.nHashFuncs = 0;
            message.nTweak = 0;
            message.nFlags = 0;
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* bytes v_data */ 1:
                        message.vData = reader.bytes();
                        break;
                    case /* uint32 n_hash_funcs */ 2:
                        message.nHashFuncs = reader.uint32();
                        break;
                    case /* uint32 n_tweak */ 3:
                        message.nTweak = reader.uint32();
                        break;
                    case /* uint32 n_flags */ 4:
                        message.nFlags = reader.uint32();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* bytes v_data = 1; */
            if (message.vData.length)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.vData);
            /* uint32 n_hash_funcs = 2; */
            if (message.nHashFuncs !== 0)
                writer.tag(2, runtime.WireType.Varint).uint32(message.nHashFuncs);
            /* uint32 n_tweak = 3; */
            if (message.nTweak !== 0)
                writer.tag(3, runtime.WireType.Varint).uint32(message.nTweak);
            /* uint32 n_flags = 4; */
            if (message.nFlags !== 0)
                writer.tag(4, runtime.WireType.Varint).uint32(message.nFlags);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.BloomFilter
     */
    const BloomFilter = new BloomFilter$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class TransactionsWithProofsResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.TransactionsWithProofsResponse", [
                { no: 1, name: "raw_transactions", kind: "message", oneof: "responses", T: () => RawTransactions },
                { no: 2, name: "instant_send_lock_messages", kind: "message", oneof: "responses", T: () => InstantSendLockMessages },
                { no: 3, name: "raw_merkle_block", kind: "scalar", oneof: "responses", T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.responses = { oneofKind: undefined };
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* org.dash.platform.dapi.v0.RawTransactions raw_transactions */ 1:
                        message.responses = {
                            oneofKind: "rawTransactions",
                            rawTransactions: RawTransactions.internalBinaryRead(reader, reader.uint32(), options, message.responses.rawTransactions)
                        };
                        break;
                    case /* org.dash.platform.dapi.v0.InstantSendLockMessages instant_send_lock_messages */ 2:
                        message.responses = {
                            oneofKind: "instantSendLockMessages",
                            instantSendLockMessages: InstantSendLockMessages.internalBinaryRead(reader, reader.uint32(), options, message.responses.instantSendLockMessages)
                        };
                        break;
                    case /* bytes raw_merkle_block */ 3:
                        message.responses = {
                            oneofKind: "rawMerkleBlock",
                            rawMerkleBlock: reader.bytes()
                        };
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* org.dash.platform.dapi.v0.RawTransactions raw_transactions = 1; */
            if (message.responses.oneofKind === "rawTransactions")
                RawTransactions.internalBinaryWrite(message.responses.rawTransactions, writer.tag(1, runtime.WireType.LengthDelimited).fork(), options).join();
            /* org.dash.platform.dapi.v0.InstantSendLockMessages instant_send_lock_messages = 2; */
            if (message.responses.oneofKind === "instantSendLockMessages")
                InstantSendLockMessages.internalBinaryWrite(message.responses.instantSendLockMessages, writer.tag(2, runtime.WireType.LengthDelimited).fork(), options).join();
            /* bytes raw_merkle_block = 3; */
            if (message.responses.oneofKind === "rawMerkleBlock")
                writer.tag(3, runtime.WireType.LengthDelimited).bytes(message.responses.rawMerkleBlock);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.TransactionsWithProofsResponse
     */
    const TransactionsWithProofsResponse = new TransactionsWithProofsResponse$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class RawTransactions$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.RawTransactions", [
                { no: 1, name: "transactions", kind: "scalar", repeat: 2 /*RepeatType.UNPACKED*/, T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.transactions = [];
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* repeated bytes transactions */ 1:
                        message.transactions.push(reader.bytes());
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* repeated bytes transactions = 1; */
            for (let i = 0; i < message.transactions.length; i++)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.transactions[i]);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.RawTransactions
     */
    const RawTransactions = new RawTransactions$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class InstantSendLockMessages$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.InstantSendLockMessages", [
                { no: 1, name: "messages", kind: "scalar", repeat: 2 /*RepeatType.UNPACKED*/, T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.messages = [];
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* repeated bytes messages */ 1:
                        message.messages.push(reader.bytes());
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* repeated bytes messages = 1; */
            for (let i = 0; i < message.messages.length; i++)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.messages[i]);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.InstantSendLockMessages
     */
    const InstantSendLockMessages = new InstantSendLockMessages$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class MasternodeListRequest$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.MasternodeListRequest", []);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.MasternodeListRequest
     */
    const MasternodeListRequest = new MasternodeListRequest$Type();
    // @generated message type with reflection information, may provide speed optimized methods
    class MasternodeListResponse$Type extends runtime.MessageType {
        constructor() {
            super("org.dash.platform.dapi.v0.MasternodeListResponse", [
                { no: 1, name: "masternode_list_diff", kind: "scalar", T: 12 /*ScalarType.BYTES*/ }
            ]);
        }
        create(value) {
            const message = globalThis.Object.create((this.messagePrototype));
            message.masternodeListDiff = new Uint8Array(0);
            if (value !== undefined)
                runtime.reflectionMergePartial(this, message, value);
            return message;
        }
        internalBinaryRead(reader, length, options, target) {
            let message = target ?? this.create(), end = reader.pos + length;
            while (reader.pos < end) {
                let [fieldNo, wireType] = reader.tag();
                switch (fieldNo) {
                    case /* bytes masternode_list_diff */ 1:
                        message.masternodeListDiff = reader.bytes();
                        break;
                    default:
                        let u = options.readUnknownField;
                        if (u === "throw")
                            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
                        let d = reader.skip(wireType);
                        if (u !== false)
                            (u === true ? runtime.UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
                }
            }
            return message;
        }
        internalBinaryWrite(message, writer, options) {
            /* bytes masternode_list_diff = 1; */
            if (message.masternodeListDiff.length)
                writer.tag(1, runtime.WireType.LengthDelimited).bytes(message.masternodeListDiff);
            let u = options.writeUnknownFields;
            if (u !== false)
                (u == true ? runtime.UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
            return writer;
        }
    }
    /**
     * @generated MessageType for protobuf message org.dash.platform.dapi.v0.MasternodeListResponse
     */
    const MasternodeListResponse = new MasternodeListResponse$Type();
    /**
     * @generated ServiceType for protobuf service org.dash.platform.dapi.v0.Core
     */
    const Core = new runtimeRpc.ServiceType("org.dash.platform.dapi.v0.Core", [
        { name: "getBlockchainStatus", options: {}, I: GetBlockchainStatusRequest, O: GetBlockchainStatusResponse },
        { name: "getMasternodeStatus", options: {}, I: GetMasternodeStatusRequest, O: GetMasternodeStatusResponse },
        { name: "getBlock", options: {}, I: GetBlockRequest, O: GetBlockResponse },
        { name: "getBestBlockHeight", options: {}, I: GetBestBlockHeightRequest, O: GetBestBlockHeightResponse },
        { name: "broadcastTransaction", options: {}, I: BroadcastTransactionRequest, O: BroadcastTransactionResponse },
        { name: "getTransaction", options: {}, I: GetTransactionRequest, O: GetTransactionResponse },
        { name: "getEstimatedTransactionFee", options: {}, I: GetEstimatedTransactionFeeRequest, O: GetEstimatedTransactionFeeResponse },
        { name: "subscribeToBlockHeadersWithChainLocks", serverStreaming: true, options: {}, I: BlockHeadersWithChainLocksRequest, O: BlockHeadersWithChainLocksResponse },
        { name: "subscribeToTransactionsWithProofs", serverStreaming: true, options: {}, I: TransactionsWithProofsRequest, O: TransactionsWithProofsResponse },
        { name: "subscribeToMasternodeList", serverStreaming: true, options: {}, I: MasternodeListRequest, O: MasternodeListResponse }
    ]);

    /**
     * @generated from protobuf service org.dash.platform.dapi.v0.Core
     */
    class CoreClient {
        _transport;
        typeName = Core.typeName;
        methods = Core.methods;
        options = Core.options;
        constructor(_transport) {
            this._transport = _transport;
        }
        /**
         * @generated from protobuf rpc: getBlockchainStatus
         */
        getBlockchainStatus(input, options) {
            const method = this.methods[0], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: getMasternodeStatus
         */
        getMasternodeStatus(input, options) {
            const method = this.methods[1], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: getBlock
         */
        getBlock(input, options) {
            const method = this.methods[2], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: getBestBlockHeight
         */
        getBestBlockHeight(input, options) {
            const method = this.methods[3], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: broadcastTransaction
         */
        broadcastTransaction(input, options) {
            const method = this.methods[4], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: getTransaction
         */
        getTransaction(input, options) {
            const method = this.methods[5], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: getEstimatedTransactionFee
         */
        getEstimatedTransactionFee(input, options) {
            const method = this.methods[6], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("unary", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: subscribeToBlockHeadersWithChainLocks
         */
        subscribeToBlockHeadersWithChainLocks(input, options) {
            const method = this.methods[7], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("serverStreaming", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: subscribeToTransactionsWithProofs
         */
        subscribeToTransactionsWithProofs(input, options) {
            const method = this.methods[8], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("serverStreaming", this._transport, method, opt, input);
        }
        /**
         * @generated from protobuf rpc: subscribeToMasternodeList
         */
        subscribeToMasternodeList(input, options) {
            const method = this.methods[9], opt = this._transport.mergeOptions(options);
            return runtimeRpc.stackIntercept("serverStreaming", this._transport, method, opt, input);
        }
    }

    const GRPC_DEFAULT_POOL_LIMIT = 5;
    const seedNodes = {
        testnet: [
            // seed-1.pshenmic.dev
            'https://158.160.14.115:1443'
        ],
        mainnet: [
            // seed-1.pshenmic.dev
            'https://158.160.14.115:443'
            // mainnet dcg seeds
            // 'https://158.160.14.115',
            // 'https://3.0.60.103',
            // 'https://34.211.174.194'
        ]
    };
    const createClient = (url, abortController) => {
        return new CoreClient(new grpcwebTransport.GrpcWebFetchTransport({
            baseUrl: url,
            abort: abortController?.signal
        }));
    };
    class GRPCConnectionPool {
        dapiUrls;
        network;
        constructor(network, grpcOptions) {
            const grpcPoolLimit = grpcOptions?.poolLimit ?? GRPC_DEFAULT_POOL_LIMIT;
            this.network = network;
            this._initialize(network, grpcPoolLimit, grpcOptions?.dapiUrl).catch(console.error);
        }
        async _initialize(network, poolLimit, dapiUrl) {
            if (typeof dapiUrl === 'string') {
                this.dapiUrls = [dapiUrl];
                return;
            }
            if (Array.isArray(dapiUrl)) {
                this.dapiUrls = dapiUrl;
                return;
            }
            if (dapiUrl != null) {
                throw new Error('Unrecognized DAPI URL');
            }
            // Add default seed nodes
            this.dapiUrls = seedNodes[network];
            // retrieve last evonodes list
            const evonodeList = await getDAPINodeList(network);
            // map it to array of dapiUrls
            const networkDAPIUrls = Object.entries(evonodeList)
                .map(([, info]) => info)
                .filter((info) => info.status === 'ENABLED')
                .map((info) => {
                const [host] = info.address.split(':');
                return `https://${host}:${info.platformHTTPPort}`;
            });
            // healthcheck nodes
            for (const url of networkDAPIUrls) {
                if (this.dapiUrls.length > poolLimit) {
                    break;
                }
                try {
                    const client = createClient(url);
                    const { response } = await client.getBlockchainStatus(GetBlockchainStatusRequest.fromJson({ v0: {} }));
                    if (response.status === GetBlockchainStatusResponse_Status.READY) {
                        this.dapiUrls.push(url);
                    }
                }
                catch (e) {
                }
            }
        }
        getClient(abortController) {
            const dapiUrl = getRandomArrayItem(this.dapiUrls);
            return createClient(dapiUrl, abortController);
        }
    }

    class PublicKey {
        inner;
        compressed;
        constructor(inner, compressed) {
            this.inner = inner;
            this.compressed = compressed;
        }
        getPublicKeyHash() {
            return bytesToHex(this.getPublicKeyHashBytes());
        }
        getPublicKeyHashBytes() {
            return SHA256RIPEMD160(this.inner);
        }
        static fromPrivateKey(privateKey) {
            const inner = secp256k1_js.secp256k1.getPublicKey(privateKey.key, privateKey.compressed);
            return new PublicKey(inner, privateKey.compressed);
        }
        getAddress(network) {
            const prefix = PubKeyHashAddressNetworkPrefix[network];
            const innerHash160 = SHA256RIPEMD160(this.inner);
            const encodable = new Uint8Array(1 + innerHash160.byteLength);
            encodable.set([prefix], 0);
            encodable.set(innerHash160, 1);
            return Base58Check.encode(encodable);
        }
        hex() {
            return bytesToHex(this.inner);
        }
        bytes() {
            return this.inner;
        }
        static fromBytes(inner) {
            if (inner.byteLength === 33) {
                return new PublicKey(inner, true);
            }
            else if (inner.byteLength === 65) {
                return new PublicKey(inner, false);
            }
            else {
                throw new Error('Incorrect bytes length for public key inner');
            }
        }
        static fromHex(hex) {
            return PublicKey.fromBytes(hexToBytes(hex));
        }
        toJSON() {
            return {
                inner: bytesToHex(this.inner),
                compressed: this.compressed
            };
        }
    }

    class PrivateKey {
        network;
        key;
        compressed;
        constructor(key, network, compressed) {
            this.key = key;
            this.network = networkValueToEnumValue(network);
            this.compressed = compressed;
        }
        getPublicKey() {
            return PublicKey.fromPrivateKey(this);
        }
        getAddress() {
            const publicKey = PublicKey.fromPrivateKey(this);
            return publicKey.getAddress(this.network);
        }
        toWIF() {
            const networkByte = WIFNetworkPrefix[this.network];
            const compressedByte = this.compressed ? 1 : 0;
            const keyBytes = new Uint8Array(34);
            keyBytes.set(new Uint8Array([networkByte]), 0);
            keyBytes.set(this.key, 1);
            keyBytes.set(new Uint8Array([compressedByte]), 33);
            return Base58Check.encode(keyBytes);
        }
        bytes() {
            return this.key;
        }
        static fromWIF(wif) {
            const wifBytes = Base58Check.decode(wif);
            const [networkByte] = wifBytes;
            const [compressed] = wifBytes.toReversed();
            const body = wifBytes.slice(1, 33);
            return new PrivateKey(body, WIFNetworkPrefix[networkByte], compressed === 1);
        }
        static fromBytes(bytes, network, compressed = true) {
            return new PrivateKey(bytes, networkValueToEnumValue(network), compressed);
        }
    }

    class Script {
        #parsedScript;
        constructor(script) {
            if (script == null) {
                this.#parsedScript = [];
                return this;
            }
            if (script instanceof Script) {
                this.#parsedScript = [...script.parsedScriptChunks];
                return this;
            }
            if (ArrayBuffer.isView(script)) {
                return Script.fromBytes(script);
            }
            if (typeof script === 'string') {
                if (/^[0-9A-Fa-f]+$/.test(script)) {
                    return Script.fromHex(script);
                }
                else {
                    return Script.fromASM(script);
                }
            }
            if (Array.isArray(script)) {
                this.#parsedScript = [...script];
                return this;
            }
        }
        get parsedScriptChunks() {
            return this.#parsedScript;
        }
        set parsedScriptChunks(chunks) {
            this.#parsedScript = chunks;
        }
        pushOpCode(opCode, data) {
            this.#parsedScript.push({
                opcode: OPCODES[opCode],
                data: data?.buffer
            });
        }
        getAddress(network = DEFAULT_NETWORK) {
            const normalNetwork = networkValueToEnumValue(network);
            if (normalNetwork > 255) {
                throw new Error('Network prefix cannot be more than 255');
            }
            const cryptoOpCodes = [OPCODES.OP_PUSHBYTES_33, OPCODES.OP_PUSHBYTES_65, OPCODES.OP_HASH160];
            const cryptoOpcodeIndex = this.parsedScriptChunks.findIndex(chunk => cryptoOpCodes.includes(chunk.opcode));
            if (cryptoOpcodeIndex === -1) {
                return undefined;
            }
            const opcodeChunk = this.parsedScriptChunks[cryptoOpcodeIndex];
            let pubKeyHash;
            const prefix = (normalNetwork ?? DEFAULT_NETWORK) === Network.Testnet ? NetworkPrefix.PubkeyPrefixTestnet : NetworkPrefix.PubkeyPrefixMainnet;
            if (opcodeChunk?.opcode === OPCODES.OP_HASH160) {
                if (opcodeChunk === undefined) {
                    return undefined;
                }
                const hashChunk = this.parsedScriptChunks[cryptoOpcodeIndex + 1];
                if (hashChunk.data == null) {
                    return undefined;
                }
                pubKeyHash = new Uint8Array(hashChunk.data);
            }
            else {
                if (opcodeChunk === undefined || opcodeChunk?.data === undefined) {
                    return undefined;
                }
                pubKeyHash = SHA256RIPEMD160(new Uint8Array(opcodeChunk.data));
            }
            const pubKeyHashWithPrefix = new Uint8Array(1 + pubKeyHash.byteLength);
            pubKeyHashWithPrefix.set(new Uint8Array([prefix]), 0);
            pubKeyHashWithPrefix.set(new Uint8Array(pubKeyHash), 1);
            return Base58Check.encode(pubKeyHashWithPrefix);
        }
        static fromBytes(bytes) {
            // OP_DUP OP_HASH160 OP_PUSHBYTES_20 a3890b802865e1cfeed7653d0fea33831d709b6f OP_EQUALVERIFY OP_CHECKSIG
            // 76a914a3890b802865e1cfeed7653d0fea33831d709b6f88ac
            // stack operator  cryptography  push_data                 data                         Bitwise Logic    cryptography
            // 76                a9              14       a3890b802865e1cfeed7653d0fea33831d709b6f      88              ac
            const chunks = [];
            const dataView = new DataView(bytes.buffer);
            for (let i = 0; i < bytes.length; i++) {
                const opcode = dataView.getUint8(i);
                const chunk = {
                    opcode
                };
                if (opcode < OPCODES.OP_PUSHDATA1 && opcode > 0) {
                    // process OP_PUSHBYTES_X
                    chunk.data = dataView.buffer.slice(i + 1, i + 1 + opcode);
                    i += opcode;
                }
                else if (opcode === OPCODES.OP_PUSHDATA1) {
                    const bytesForPush = dataView.getUint8(i + 1);
                    chunk.data = dataView.buffer.slice(i + 1, i + 1 + bytesForPush);
                    i += bytesForPush;
                }
                else if (opcode === OPCODES.OP_PUSHDATA2) {
                    const bytesForPush = dataView.getUint16(i + 1, true);
                    chunk.data = dataView.buffer.slice(i + 3, i + 3 + bytesForPush);
                    i += bytesForPush + 2;
                }
                else if (opcode === OPCODES.OP_PUSHDATA4) {
                    const bytesForPush = dataView.getUint32(i + 1, true);
                    chunk.data = dataView.buffer.slice(i + 5, i + 5 + bytesForPush);
                    i += bytesForPush + 4;
                }
                chunks.push(chunk);
            }
            return new Script(chunks);
        }
        static fromHex(hex) {
            return this.fromBytes(hexToBytes(hex));
        }
        static fromASM(asm) {
            const scriptChunks = [];
            const chunks = asm.split(' ');
            for (let i = 0; i < chunks.length; i++) {
                const opcode = OPCODES[chunks[i]];
                if (opcode === undefined) {
                    throw new Error('Cannot parse asm string');
                }
                if (opcode > 0 && opcode <= OPCODES.OP_PUSHDATA4) {
                    scriptChunks.push({
                        opcode,
                        data: hexToBytes(chunks[i + 1]).buffer
                    });
                    i++;
                }
                else {
                    scriptChunks.push({
                        opcode
                    });
                }
            }
            return new Script(scriptChunks);
        }
        ASMString() {
            const out = [];
            for (let i = 0; i < this.#parsedScript.length; i++) {
                const chunk = this.#parsedScript[i];
                const opcodeHumanReadable = Object.keys(OPCODES)[Object.values(OPCODES).indexOf(chunk.opcode)];
                const dataHumanReadable = (chunk.data != null) ? bytesToHex(new Uint8Array(chunk.data)) : undefined;
                out.push(opcodeHumanReadable);
                if (dataHumanReadable != null) {
                    out.push(dataHumanReadable);
                }
            }
            return out.join(' ');
        }
        bytes() {
            let out = new Uint8Array(new ArrayBuffer(0));
            for (let i = 0; i < this.#parsedScript.length; i++) {
                const chunk = this.#parsedScript[i];
                const { data } = chunk;
                const dataLength = data?.byteLength ?? 0;
                const tmpBytes = new Uint8Array(1 + dataLength + out.byteLength);
                tmpBytes.set(out, 0);
                tmpBytes.set([chunk.opcode], out.byteLength);
                if (chunk.data != null) {
                    tmpBytes.set(new Uint8Array(chunk.data), out.byteLength + 1);
                }
                out = tmpBytes;
            }
            return out;
        }
        hex() {
            const bytes = this.bytes();
            return bytesToHex(bytes);
        }
    }

    class Input {
        txId;
        vOut;
        scriptSig;
        sequence;
        constructor(txId, vOut, scriptSig, sequence) {
            if (typeof txId === 'string') {
                this.txId = txId;
            }
            else if (ArrayBuffer.isView(txId)) {
                this.txId = bytesToHex(txId);
            }
            else {
                this.txId = bytesToHex(new Uint8Array(32));
            }
            this.vOut = vOut;
            this.scriptSig = scriptSig;
            this.sequence = sequence;
        }
        getTxIdHex() {
            return this.txId;
        }
        bytes() {
            const txIdBytes = hexToBytes(this.txId);
            const vOutBytes = new DataView(new ArrayBuffer(4));
            const scriptSigBytes = this.scriptSig.bytes();
            const scriptSigSizeBytes = encodeCompactSize(scriptSigBytes.byteLength);
            const sequenceBytes = new DataView(new ArrayBuffer(4));
            vOutBytes.setUint32(0, this.vOut, true);
            sequenceBytes.setUint32(0, this.sequence, true);
            const bytes = new Uint8Array(txIdBytes.byteLength + vOutBytes.byteLength + scriptSigBytes.byteLength + scriptSigSizeBytes.byteLength + sequenceBytes.byteLength);
            bytes.set(txIdBytes.toReversed(), 0);
            bytes.set(new Uint8Array(vOutBytes.buffer), txIdBytes.byteLength);
            bytes.set(scriptSigSizeBytes, txIdBytes.byteLength + vOutBytes.byteLength);
            bytes.set(scriptSigBytes, txIdBytes.byteLength + vOutBytes.byteLength + scriptSigSizeBytes.byteLength);
            bytes.set(new Uint8Array(sequenceBytes.buffer), txIdBytes.byteLength + vOutBytes.byteLength + scriptSigBytes.byteLength + scriptSigSizeBytes.byteLength);
            return bytes;
        }
        static fromBytes(bytes) {
            const txIdBytes = bytes.slice(0, 32);
            const vOutBytes = new DataView(bytes.slice(32, 36).buffer);
            const scriptSigSize = decodeCompactSize(36, bytes);
            const scriptBytesSize = getCompactVariableSize(scriptSigSize);
            const scriptSigBytes = bytes.slice(36 + scriptBytesSize, 36 + scriptBytesSize + Number(scriptSigSize));
            const sequenceBytes = new DataView(bytes.slice(36 + scriptBytesSize + Number(scriptSigSize), 36 + scriptBytesSize + Number(scriptSigSize) + 4).buffer);
            return new Input(txIdBytes.toReversed(), vOutBytes.getUint32(0, true), Script.fromBytes(scriptSigBytes), sequenceBytes.getUint32(0, true));
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        static fromHex(hex) {
            return Input.fromBytes(hexToBytes(hex));
        }
        toJSON() {
            return {
                txId: this.txId,
                vOut: this.vOut,
                scriptSig: this.scriptSig.ASMString(),
                sequence: this.sequence
            };
        }
    }

    class Output {
        satoshis;
        script;
        constructor(satoshis, script) {
            this.satoshis = satoshis;
            this.script = script ?? new Script();
        }
        static fromBytes(bytes) {
            const properties = new DataView(bytes.buffer);
            const satoshis = properties.getBigUint64(0, true);
            const scriptSize = decodeCompactSize(8, bytes);
            const scriptPadding = 8 + getCompactVariableSize(scriptSize);
            const script = new Script(new Uint8Array(properties.buffer.slice(scriptPadding, scriptPadding + Number(scriptSize))));
            return new Output(satoshis, script);
        }
        static fromHex(hex) {
            return Output.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const scriptBytes = this.script.bytes();
            const scriptSize = encodeCompactSize(scriptBytes.byteLength);
            const dataView = new DataView(new ArrayBuffer(8));
            dataView.setBigUint64(0, this.satoshis, true);
            const bytes = new Uint8Array(dataView.byteLength + scriptBytes.byteLength + getCompactVariableSize(scriptBytes.byteLength));
            bytes.set(new Uint8Array(dataView.buffer), 0);
            bytes.set(scriptSize, dataView.buffer.byteLength);
            bytes.set(scriptBytes, dataView.byteLength + scriptSize.byteLength);
            return bytes;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        generateP2PKH(address) {
            const script = new Script();
            script.pushOpCode('OP_DUP');
            script.pushOpCode('OP_HASH160');
            script.pushOpCode('OP_PUSHBYTES_20', addressToPublicKeyHash(address));
            script.pushOpCode('OP_EQUALVERIFY');
            script.pushOpCode('OP_CHECKSIG');
            this.script = script;
        }
        getAddress(network = DEFAULT_NETWORK) {
            const normalNetwork = networkValueToEnumValue(network);
            if (normalNetwork > 255) {
                throw new Error('Network prefix cannot be more than 255');
            }
            // TODO: add other codes
            const cryptoOpCodes = [OPCODES.OP_HASH160];
            const cryptoOpcodeIndex = this.script.parsedScriptChunks.findIndex(chunk => cryptoOpCodes.includes(chunk.opcode));
            if (cryptoOpcodeIndex === -1) {
                return undefined;
            }
        }
        toJSON() {
            return {
                satoshis: String(this.satoshis),
                script: this.script.ASMString()
            };
        }
    }

    class OutPoint {
        txId;
        vOut;
        constructor(txId, vOut) {
            this.txId = txId;
            this.vOut = vOut;
        }
        bytes() {
            const out = new Uint8Array(36);
            const vOutView = new DataView(new ArrayBuffer(4));
            vOutView.setUint32(0, this.vOut, true);
            const txIdBytes = hexToBytes(this.txId);
            out.set(txIdBytes.toReversed(), 0);
            out.set(new Uint8Array(vOutView.buffer), txIdBytes.byteLength);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const txId = bytes.slice(0, 32);
            const vOut = dataView.getUint32(32, true);
            return new OutPoint(bytesToHex(txId.toReversed()), vOut);
        }
        static fromHex(hex) {
            return OutPoint.fromBytes(hexToBytes(hex));
        }
        toJSON() {
            return {
                txId: this.txId,
                vOut: this.vOut
            };
        }
    }

    class ProRegTX {
        version;
        // type 0 = mn, type 1 = evo
        type;
        mode;
        collateralOutpoint;
        ipAddress;
        port;
        // netInfo: Uint8Array
        keyIdOwner;
        keyIdVoting;
        pubKeyOperator;
        operatorReward;
        scriptPayout;
        inputsHash;
        // only if version == 2
        platformNodeID;
        platformP2PPort;
        platformHTTPPort;
        payloadSig;
        constructor(version, type, mode, collateralOutpoint, ipAddress, port, /* netInfo: Uint8Array, */ keyIdOwner, pubKeyOperator, keyIdVoting, operatorReward, scriptPayout, inputsHash, platformNodeID, platformP2PPort, platformHTTPPort, payloadSig) {
            this.version = version;
            this.type = type;
            this.mode = mode;
            this.collateralOutpoint = collateralOutpoint;
            this.ipAddress = ipAddress;
            this.port = port;
            // this.netInfo = netInfo;
            this.keyIdOwner = keyIdOwner;
            this.pubKeyOperator = pubKeyOperator;
            this.keyIdVoting = keyIdVoting;
            this.operatorReward = operatorReward;
            this.scriptPayout = scriptPayout;
            this.inputsHash = inputsHash;
            this.platformNodeID = platformNodeID;
            this.platformP2PPort = platformP2PPort;
            this.platformHTTPPort = platformHTTPPort;
            this.payloadSig = payloadSig;
        }
        getOwnerAddress(network = DEFAULT_NETWORK) {
            return publicKeyHashToAddress(hexToBytes(this.keyIdOwner), network);
        }
        getVotingAddress(network = DEFAULT_NETWORK) {
            return publicKeyHashToAddress(hexToBytes(this.keyIdVoting), network);
        }
        getPayoutAddress(network = DEFAULT_NETWORK) {
            return this.scriptPayout.getAddress(network);
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const type = dataView.getUint16(2, true);
            const mode = dataView.getUint16(4, true);
            const colateralOutpoint = OutPoint.fromBytes(bytes.slice(6, 42));
            let ipAddress;
            let port;
            if (version < 3) {
                ipAddress = bytes.slice(42, 58);
                port = dataView.getUint16(58, false);
            }
            else {
                throw new Error(`Unsupported version of ProRegTX: ${version}`);
            }
            const keyIdOwner = bytes.slice(60, 80);
            const pubKeyOperator = bytes.slice(80, 128);
            const keyIdVoting = bytes.slice(128, 148);
            const operatorReward = dataView.getUint16(148, true);
            const scriptPayoutSize = decodeCompactSize(150, bytes);
            const scriptPayout = Script.fromBytes(bytes.slice(150 + getCompactVariableSize(scriptPayoutSize), 150 + getCompactVariableSize(scriptPayoutSize) + Number(scriptPayoutSize)));
            const inputsHashOffset = 150 + getCompactVariableSize(scriptPayoutSize) + Number(scriptPayoutSize);
            const inputsHash = bytes.slice(inputsHashOffset, inputsHashOffset + 32);
            let platformNodeID;
            let platformP2PPort;
            let platformHTTPPort;
            let payloadSigOffset = inputsHashOffset + 32;
            if (version === 2) {
                platformNodeID = bytes.slice(inputsHashOffset + 32, inputsHashOffset + 32 + 20);
                platformP2PPort = dataView.getUint16(inputsHashOffset + 32 + 20, true);
                platformHTTPPort = dataView.getUint16(inputsHashOffset + 32 + 22, true);
                payloadSigOffset += 24;
            }
            const payloadSigSize = decodeCompactSize(payloadSigOffset, bytes);
            const payloadSig = bytes.slice(payloadSigOffset + getCompactVariableSize(payloadSigSize), payloadSigOffset + getCompactVariableSize(payloadSigSize) + Number(payloadSigSize));
            return new ProRegTX(version, type, mode, colateralOutpoint, bytesToIp(ipAddress), port, bytesToHex(keyIdOwner), bytesToHex(pubKeyOperator), bytesToHex(keyIdVoting), operatorReward, scriptPayout, bytesToHex(inputsHash.toReversed()), bytesToHex(platformNodeID.toReversed()), platformP2PPort, platformHTTPPort, bytesToHex(payloadSig));
        }
        static fromHex(hex) {
            return ProRegTX.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const typeBytes = new Uint8Array(2);
            const modeBytes = new Uint8Array(2);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(typeBytes.buffer).setUint16(0, this.type, true);
            new DataView(modeBytes.buffer).setUint16(0, this.mode, true);
            const collateralOutpointBytes = this.collateralOutpoint.bytes();
            const ipAddressBytes = ipToBytes(this.ipAddress);
            const portBytes = new Uint8Array(2);
            new DataView(portBytes.buffer).setUint16(0, this.port, false);
            const keyIdBytes = hexToBytes(this.keyIdOwner);
            const pubKeyOperatorBytes = hexToBytes(this.pubKeyOperator);
            const keyIdVotingBytes = hexToBytes(this.keyIdVoting);
            const operatorRewardBytes = new Uint8Array(2);
            new DataView(operatorRewardBytes.buffer).setUint16(0, this.operatorReward, true);
            const scriptPayoutBytes = this.scriptPayout.bytes();
            const scriptPayoutSizeBytes = encodeCompactSize(scriptPayoutBytes.byteLength);
            const inputsHashBytes = hexToBytes(this.inputsHash).toReversed();
            let platformNodeIDBytes = new Uint8Array(0);
            let platformP2PPortBytes = new Uint8Array(0);
            let platformHTTPPortBytes = new Uint8Array(0);
            if (this.version === 2) {
                platformNodeIDBytes = hexToBytes(this.platformNodeID ?? '').toReversed();
                platformP2PPortBytes = new Uint8Array(2);
                platformHTTPPortBytes = new Uint8Array(2);
                new DataView(platformP2PPortBytes.buffer).setUint16(0, this.platformP2PPort ?? 0, true);
                new DataView(platformHTTPPortBytes.buffer).setUint16(0, this.platformHTTPPort ?? 0, true);
            }
            const payloadSigBytes = hexToBytes(this.payloadSig);
            const payloadSigSizeBytes = encodeCompactSize(payloadSigBytes.byteLength);
            const ipAddressOffset = versionBytes.byteLength + typeBytes.byteLength + modeBytes.byteLength + collateralOutpointBytes.byteLength;
            const pubKeyOperatorOffset = ipAddressOffset + ipAddressBytes.byteLength + portBytes.byteLength + keyIdBytes.byteLength;
            const scriptOffset = pubKeyOperatorOffset + pubKeyOperatorBytes.byteLength + keyIdVotingBytes.byteLength + operatorRewardBytes.byteLength;
            const platformInfoOffset = scriptOffset + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength + inputsHashBytes.byteLength;
            const payloadSigOffset = platformInfoOffset + platformNodeIDBytes.byteLength + platformHTTPPortBytes.byteLength + platformHTTPPortBytes.byteLength;
            const outBytes = new Uint8Array(182 + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength + platformNodeIDBytes.byteLength + platformP2PPortBytes.byteLength + platformHTTPPortBytes.byteLength + payloadSigSizeBytes.byteLength + payloadSigBytes.byteLength);
            outBytes.set(versionBytes, 0);
            outBytes.set(typeBytes, versionBytes.byteLength);
            outBytes.set(modeBytes, versionBytes.byteLength + typeBytes.byteLength);
            outBytes.set(collateralOutpointBytes, versionBytes.byteLength + typeBytes.byteLength + modeBytes.byteLength);
            outBytes.set(ipAddressBytes, ipAddressOffset);
            outBytes.set(portBytes, ipAddressOffset + ipAddressBytes.byteLength);
            outBytes.set(keyIdBytes, ipAddressOffset + ipAddressBytes.byteLength + portBytes.byteLength);
            outBytes.set(pubKeyOperatorBytes, pubKeyOperatorOffset);
            outBytes.set(keyIdVotingBytes, pubKeyOperatorOffset + pubKeyOperatorBytes.byteLength);
            outBytes.set(operatorRewardBytes, pubKeyOperatorOffset + pubKeyOperatorBytes.byteLength + keyIdVotingBytes.byteLength);
            outBytes.set(scriptPayoutSizeBytes, scriptOffset);
            outBytes.set(scriptPayoutBytes, scriptOffset + scriptPayoutSizeBytes.byteLength);
            outBytes.set(inputsHashBytes, scriptOffset + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength);
            outBytes.set(platformNodeIDBytes, platformInfoOffset);
            outBytes.set(platformP2PPortBytes, platformInfoOffset + platformNodeIDBytes.byteLength);
            outBytes.set(platformHTTPPortBytes, platformInfoOffset + platformNodeIDBytes.byteLength + platformHTTPPortBytes.byteLength);
            outBytes.set(payloadSigSizeBytes, payloadSigOffset);
            outBytes.set(payloadSigBytes, payloadSigOffset + payloadSigSizeBytes.byteLength);
            return outBytes;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                collateralOutpoint: this.collateralOutpoint,
                inputsHash: this.inputsHash,
                ipAddress: this.ipAddress,
                keyIdOwner: this.keyIdOwner,
                keyIdVoting: this.keyIdVoting,
                mode: this.mode,
                operatorReward: this.operatorReward,
                port: this.port,
                pubKeyOperator: this.pubKeyOperator,
                scriptPayout: this.scriptPayout.ASMString(),
                type: this.type,
                version: this.version
            };
        }
    }

    class ProUpServTx {
        version;
        // type 0 = mn, type 1 = evo
        type;
        proTxHash;
        ipAddress;
        port;
        // netInfo: Uint8Array
        scriptOperatorPayout;
        inputsHash;
        // only if version == 2
        platformNodeID;
        platformP2PPort;
        platformHTTPPort;
        payloadSig;
        constructor(version, type, proTxHash, ipAddress, port, /* netInfo: Uint8Array, */ scriptOperatorPayout, inputsHash, platformNodeID, platformP2PPort, platformHTTPPort, payloadSig) {
            this.version = version;
            this.type = type;
            this.proTxHash = proTxHash;
            this.ipAddress = ipAddress;
            this.port = port;
            // this.netInfo = netInfo;
            this.scriptOperatorPayout = scriptOperatorPayout;
            this.inputsHash = inputsHash;
            this.platformNodeID = platformNodeID;
            this.platformP2PPort = platformP2PPort;
            this.platformHTTPPort = platformHTTPPort;
            this.payloadSig = payloadSig;
        }
        getOperatorPayoutAddress(network = DEFAULT_NETWORK) {
            return this.scriptOperatorPayout.getAddress(network);
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const type = dataView.getUint16(2, true);
            const proTxHash = bytes.slice(4, 36);
            let ipAddress;
            let port;
            if (version < 3) {
                ipAddress = bytes.slice(36, 52);
                port = dataView.getUint16(52, false);
            }
            else {
                throw new Error(`Unsupported version of ProUpServTX: ${version}`);
            }
            const scriptPayoutSize = decodeCompactSize(54, bytes);
            const scriptOperatorPayout = Script.fromBytes(bytes.slice(54 + getCompactVariableSize(scriptPayoutSize), 54 + getCompactVariableSize(scriptPayoutSize) + Number(scriptPayoutSize)));
            const inputsHashOffset = 54 + getCompactVariableSize(scriptPayoutSize) + Number(scriptPayoutSize);
            const inputsHash = bytes.slice(inputsHashOffset, inputsHashOffset + 32);
            let platformNodeID;
            let platformP2PPort;
            let platformHTTPPort;
            let payloadSigOffset = inputsHashOffset + 32;
            if (version === 2) {
                platformNodeID = bytes.slice(inputsHashOffset + 32, inputsHashOffset + 32 + 20);
                platformP2PPort = dataView.getUint16(inputsHashOffset + 32 + 20, true);
                platformHTTPPort = dataView.getUint16(inputsHashOffset + 32 + 22, true);
                payloadSigOffset += 24;
            }
            const payloadSig = bytes.slice(payloadSigOffset, payloadSigOffset + 96);
            return new ProUpServTx(version, type, bytesToHex(proTxHash.toReversed()), bytesToIp(ipAddress), port, scriptOperatorPayout, bytesToHex(inputsHash.toReversed()), bytesToHex(platformNodeID.toReversed()), platformP2PPort, platformHTTPPort, bytesToHex(payloadSig));
        }
        static fromHex(hex) {
            return ProUpServTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const typeBytes = new Uint8Array(2);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(typeBytes.buffer).setUint16(0, this.type, true);
            const proTxHashBytes = new Uint8Array(hexToBytes(this.proTxHash).toReversed());
            const ipAddressBytes = ipToBytes(this.ipAddress);
            const portBytes = new Uint8Array(2);
            new DataView(portBytes.buffer).setUint16(0, this.port, false);
            const scriptOperatorPayoutBytes = this.scriptOperatorPayout.bytes();
            const scriptOperatorPayoutSizeBytes = encodeCompactSize(scriptOperatorPayoutBytes.byteLength);
            const inputsHashBytes = hexToBytes(this.inputsHash).toReversed();
            let platformNodeIDBytes = new Uint8Array(0);
            let platformP2PPortBytes = new Uint8Array(0);
            let platformHTTPPortBytes = new Uint8Array(0);
            if (this.version === 2) {
                platformNodeIDBytes = hexToBytes(this.platformNodeID ?? '').toReversed();
                platformP2PPortBytes = new Uint8Array(2);
                platformHTTPPortBytes = new Uint8Array(2);
                new DataView(platformP2PPortBytes.buffer).setUint16(0, this.platformP2PPort ?? 0, true);
                new DataView(platformHTTPPortBytes.buffer).setUint16(0, this.platformHTTPPort ?? 0, true);
            }
            const payloadSigBytes = hexToBytes(this.payloadSig);
            const outBytes = new Uint8Array(54 + scriptOperatorPayoutSizeBytes.byteLength + scriptOperatorPayoutBytes.byteLength + inputsHashBytes.byteLength + platformNodeIDBytes.byteLength + platformP2PPortBytes.byteLength + platformHTTPPortBytes.byteLength + payloadSigBytes.byteLength);
            const payoutScriptOffset = versionBytes.byteLength + typeBytes.byteLength + proTxHashBytes.byteLength + ipAddressBytes.byteLength + portBytes.byteLength;
            const platformInfoOffset = payoutScriptOffset + scriptOperatorPayoutSizeBytes.byteLength + scriptOperatorPayoutBytes.byteLength + inputsHashBytes.byteLength;
            const payloadSigOffset = platformInfoOffset + platformNodeIDBytes.byteLength + platformP2PPortBytes.byteLength + platformHTTPPortBytes.byteLength;
            outBytes.set(versionBytes, 0);
            outBytes.set(typeBytes, versionBytes.byteLength);
            outBytes.set(proTxHashBytes, versionBytes.byteLength + typeBytes.byteLength);
            outBytes.set(ipAddressBytes, versionBytes.byteLength + typeBytes.byteLength + proTxHashBytes.byteLength);
            outBytes.set(portBytes, versionBytes.byteLength + typeBytes.byteLength + proTxHashBytes.byteLength + ipAddressBytes.byteLength);
            outBytes.set(scriptOperatorPayoutSizeBytes, payoutScriptOffset);
            outBytes.set(scriptOperatorPayoutBytes, payoutScriptOffset + scriptOperatorPayoutSizeBytes.byteLength);
            outBytes.set(inputsHashBytes, payoutScriptOffset + scriptOperatorPayoutSizeBytes.byteLength + scriptOperatorPayoutBytes.byteLength);
            outBytes.set(platformNodeIDBytes, platformInfoOffset);
            outBytes.set(platformP2PPortBytes, platformInfoOffset + platformNodeIDBytes.byteLength);
            outBytes.set(platformHTTPPortBytes, platformInfoOffset + platformNodeIDBytes.byteLength + platformP2PPortBytes.byteLength);
            outBytes.set(payloadSigBytes, payloadSigOffset);
            return outBytes;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                payloadSig: this.payloadSig,
                proTxHash: this.proTxHash,
                scriptOperatorPayout: this.scriptOperatorPayout.ASMString(),
                inputsHash: this.inputsHash,
                ipAddress: this.ipAddress,
                port: this.port,
                type: this.type,
                version: this.version
            };
        }
    }

    class ProUpRegTx {
        version;
        proTxHash;
        mode;
        keyIdVoting;
        pubKeyOperator;
        scriptPayout;
        inputsHash;
        payloadSig;
        constructor(version, proTxHash, mode, pubKeyOperator, keyIdVoting, scriptPayout, inputsHash, payloadSig) {
            this.version = version;
            this.proTxHash = proTxHash;
            this.mode = mode;
            this.pubKeyOperator = pubKeyOperator;
            this.keyIdVoting = keyIdVoting;
            this.scriptPayout = scriptPayout;
            this.inputsHash = inputsHash;
            this.payloadSig = payloadSig;
        }
        getVotingAddress(network = DEFAULT_NETWORK) {
            return publicKeyHashToAddress(hexToBytes(this.keyIdVoting), network);
        }
        getPayoutAddress(network = DEFAULT_NETWORK) {
            return this.scriptPayout.getAddress(network);
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const proTxHash = bytes.slice(2, 34);
            const mode = dataView.getUint16(34, true);
            const pubKeyOperator = bytes.slice(36, 84);
            const keyIdVoting = bytes.slice(84, 104);
            const scriptPayoutSize = decodeCompactSize(104, bytes);
            const scriptPayout = Script.fromBytes(bytes.slice(104 + getCompactVariableSize(scriptPayoutSize), 104 + getCompactVariableSize(scriptPayoutSize) + Number(scriptPayoutSize)));
            const inputsHashOffset = 104 + getCompactVariableSize(scriptPayoutSize) + Number(scriptPayoutSize);
            const inputsHash = bytes.slice(inputsHashOffset, inputsHashOffset + 32);
            const payloadSigSize = decodeCompactSize(inputsHashOffset + 32, bytes);
            const payloadSig = bytes.slice(inputsHashOffset + 32 + getCompactVariableSize(payloadSigSize), inputsHashOffset + 32 + getCompactVariableSize(payloadSigSize) + Number(payloadSigSize));
            return new ProUpRegTx(version, bytesToHex(proTxHash.toReversed()), mode, bytesToHex(pubKeyOperator), bytesToHex(keyIdVoting), scriptPayout, bytesToHex(inputsHash.toReversed()), bytesToHex(payloadSig));
        }
        static fromHex(hex) {
            return ProUpRegTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const modeBytes = new Uint8Array(2);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(modeBytes.buffer).setUint16(0, this.mode, true);
            const proTxHashBytes = new Uint8Array(32);
            proTxHashBytes.set(hexToBytes(this.proTxHash).toReversed());
            const pubKeyOperatorBytes = new Uint8Array(48);
            pubKeyOperatorBytes.set(hexToBytes(this.pubKeyOperator));
            const keyIdVotingBytes = new Uint8Array(20);
            keyIdVotingBytes.set(hexToBytes(this.keyIdVoting));
            const scriptPayoutBytes = this.scriptPayout.bytes();
            const scriptPayoutSizeBytes = encodeCompactSize(scriptPayoutBytes.byteLength);
            const inputsHashBytes = new Uint8Array(32);
            inputsHashBytes.set(hexToBytes(this.inputsHash).toReversed());
            const payloadSigBytes = hexToBytes(this.payloadSig);
            const payloadSigSizeBytes = encodeCompactSize(payloadSigBytes.byteLength);
            const outBytes = new Uint8Array(136 + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength + payloadSigSizeBytes.byteLength + payloadSigBytes.byteLength);
            outBytes.set(versionBytes, 0);
            outBytes.set(proTxHashBytes, 2);
            outBytes.set(modeBytes, 34);
            outBytes.set(pubKeyOperatorBytes, 36);
            outBytes.set(keyIdVotingBytes, 84);
            outBytes.set(scriptPayoutSizeBytes, 104);
            outBytes.set(scriptPayoutBytes, 104 + scriptPayoutSizeBytes.byteLength);
            outBytes.set(inputsHashBytes, 104 + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength);
            outBytes.set(payloadSigSizeBytes, 136 + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength);
            outBytes.set(payloadSigBytes, 136 + scriptPayoutSizeBytes.byteLength + scriptPayoutBytes.byteLength + payloadSigSizeBytes.byteLength);
            return outBytes;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                inputsHash: this.inputsHash,
                keyIdVoting: this.keyIdVoting,
                mode: this.mode,
                payloadSig: this.payloadSig,
                proTxHash: this.proTxHash,
                pubKeyOperator: this.pubKeyOperator,
                scriptPayout: this.scriptPayout.ASMString(),
                version: this.version
            };
        }
    }

    class ProUpRevTx {
        version;
        proTxHash;
        reason;
        inputsHash;
        payloadSig;
        constructor(version, proTxHash, reason, inputsHash, payloadSig) {
            this.version = version;
            this.proTxHash = proTxHash;
            this.reason = reason;
            this.inputsHash = inputsHash;
            this.payloadSig = payloadSig;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const proTxHash = bytes.slice(2, 34);
            const reason = dataView.getUint16(34, true);
            const inputsHash = bytes.slice(36, 68);
            const payloadSig = bytes.slice(68, 68 + 96);
            return new ProUpRevTx(version, bytesToHex(proTxHash.toReversed()), reason, bytesToHex(inputsHash.toReversed()), bytesToHex(payloadSig));
        }
        static fromHex(hex) {
            return ProUpRevTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const reasonBytes = new Uint8Array(2);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(reasonBytes.buffer).setUint16(0, this.reason, true);
            const proTxHashBytes = new Uint8Array(32);
            proTxHashBytes.set(hexToBytes(this.proTxHash).toReversed());
            const inputsHashBytes = new Uint8Array(32);
            inputsHashBytes.set(hexToBytes(this.inputsHash).toReversed());
            const payloadSigBytes = hexToBytes(this.payloadSig);
            const outBytes = new Uint8Array(164);
            outBytes.set(versionBytes, 0);
            outBytes.set(proTxHashBytes, 2);
            outBytes.set(reasonBytes, 34);
            outBytes.set(inputsHashBytes, 36);
            outBytes.set(payloadSigBytes, 68);
            return outBytes;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                inputsHash: this.inputsHash,
                payloadSig: this.payloadSig,
                proTxHash: this.proTxHash,
                reason: this.reason,
                version: this.version
            };
        }
    }

    class CbTx {
        version;
        height;
        merkleRootMNList;
        merkleRootQuorums;
        bestCLHeightDiff;
        bestCLSignature;
        creditPoolBalance;
        constructor(version, height, merkleRootMNList, merkleRootQuorums, bestCLHeightDiff, bestCLSignature, creditPoolBalance) {
            this.version = version;
            this.height = height;
            this.merkleRootMNList = merkleRootMNList;
            this.merkleRootQuorums = merkleRootQuorums;
            this.bestCLHeightDiff = bestCLHeightDiff;
            this.bestCLSignature = bestCLSignature;
            this.creditPoolBalance = creditPoolBalance;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const height = dataView.getUint32(2, true);
            const merkleRootMNList = bytes.slice(6, 38);
            let merkleRootQuorums;
            if (version >= 2) {
                merkleRootQuorums = bytes.slice(38, 70);
            }
            let bestCLHeightDiff;
            let bestCLSignature;
            let creditPoolBalance;
            if (version >= 3) {
                bestCLHeightDiff = BigInt(decodeCompactSize(70, bytes));
                bestCLSignature = bytesToHex(bytes.slice(70 + getCompactVariableSize(bestCLHeightDiff), 70 + getCompactVariableSize(bestCLHeightDiff) + 96));
                creditPoolBalance = dataView.getBigInt64(70 + getCompactVariableSize(bestCLHeightDiff) + 96, true);
            }
            return new CbTx(version, height, bytesToHex(merkleRootMNList.toReversed()), merkleRootQuorums != null ? bytesToHex(merkleRootQuorums.toReversed()) : undefined, bestCLHeightDiff, bestCLSignature, creditPoolBalance);
        }
        static fromHex(hex) {
            return CbTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const heightBytes = new Uint8Array(4);
            const creditPoolBalanceBytes = new Uint8Array(this.version >= 3 ? 8 : 0);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(heightBytes.buffer).setUint32(0, this.height, true);
            const merkleRootMNListBytes = new Uint8Array(32);
            merkleRootMNListBytes.set(hexToBytes(this.merkleRootMNList).toReversed(), 0);
            let merkleRootQuorumsBytes = new Uint8Array(0);
            if (this.version >= 2) {
                merkleRootQuorumsBytes = new Uint8Array(32);
                merkleRootQuorumsBytes.set(hexToBytes(this.merkleRootQuorums ?? '').toReversed());
            }
            let bestCLHeightDiffBytes = new Uint8Array(0);
            let bestCLSignatureBytes = new Uint8Array(0);
            if (this.version >= 3) {
                new DataView(creditPoolBalanceBytes.buffer).setBigInt64(0, this.creditPoolBalance ?? 0n, true);
                bestCLHeightDiffBytes = encodeCompactSize(this.bestCLHeightDiff ?? 0);
                bestCLSignatureBytes = new Uint8Array(96);
                bestCLSignatureBytes.set(hexToBytes(this.bestCLSignature ?? ''));
            }
            const out = new Uint8Array(38 + merkleRootQuorumsBytes.byteLength + bestCLHeightDiffBytes.byteLength + bestCLSignatureBytes.byteLength + creditPoolBalanceBytes.byteLength);
            out.set(versionBytes, 0);
            out.set(heightBytes, versionBytes.byteLength);
            out.set(merkleRootMNListBytes, versionBytes.byteLength + heightBytes.byteLength);
            out.set(merkleRootQuorumsBytes, versionBytes.byteLength + heightBytes.byteLength + merkleRootMNListBytes.byteLength);
            out.set(bestCLHeightDiffBytes, versionBytes.byteLength + heightBytes.byteLength + merkleRootMNListBytes.byteLength + merkleRootQuorumsBytes.byteLength);
            out.set(bestCLSignatureBytes, versionBytes.byteLength + heightBytes.byteLength + merkleRootMNListBytes.byteLength + merkleRootQuorumsBytes.byteLength + bestCLHeightDiffBytes.byteLength);
            out.set(creditPoolBalanceBytes, versionBytes.byteLength + heightBytes.byteLength + merkleRootMNListBytes.byteLength + merkleRootQuorumsBytes.byteLength + bestCLHeightDiffBytes.byteLength + bestCLSignatureBytes.byteLength);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                version: this.version,
                height: this.height,
                merkleRootMNList: this.merkleRootMNList,
                merkleRootQuorums: this.merkleRootQuorums ?? null,
                bestCLHeightDiff: this.bestCLHeightDiff != null ? String(this.bestCLHeightDiff) : null,
                bestCLSignature: this.bestCLSignature ?? null,
                creditPoolBalance: this.creditPoolBalance != null ? String(this.creditPoolBalance) : null
            };
        }
    }

    class QfCommit {
        version;
        llmqType;
        quorumHash;
        quorumIndex;
        signers;
        validMembers;
        quorumPublicKey;
        quorumVvecHash;
        quorumSig;
        sig;
        constructor(version, llmqType, quorumHash, signers, validMembers, quorumPublicKey, quorumVvecHash, quorumSig, sig, quorumIndex) {
            this.version = version;
            this.llmqType = llmqType;
            this.quorumHash = quorumHash;
            this.quorumIndex = quorumIndex;
            this.signers = signers;
            this.validMembers = validMembers;
            this.quorumPublicKey = quorumPublicKey;
            this.quorumVvecHash = quorumVvecHash;
            this.quorumSig = quorumSig;
            this.sig = sig;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const llmqType = dataView.getUint8(2);
            const quorumHash = bytes.slice(3, 35);
            let signersOffset = 35;
            let quorumIndex;
            if (version >= 2) {
                quorumIndex = dataView.getUint16(35, true);
                signersOffset += 2;
            }
            const signersBitsSize = decodeCompactSize(signersOffset, bytes);
            // https://docs.dash.org/en/stable/docs/core/reference/p2p-network-quorum-messages.html#qfcommit
            const signersSize = (Number(signersBitsSize) + 7) / 8;
            const signers = bytes.slice(signersOffset + getCompactVariableSize(signersBitsSize), signersOffset + getCompactVariableSize(signersBitsSize) + signersSize);
            const validMembersOffset = signersOffset + getCompactVariableSize(signersBitsSize) + signersSize;
            const validMembersBitsSize = decodeCompactSize(validMembersOffset, bytes);
            // https://docs.dash.org/en/stable/docs/core/reference/p2p-network-quorum-messages.html#qfcommit
            const validMembersSize = (Number(validMembersBitsSize) + 7) / 8;
            const validMembers = bytes.slice(validMembersOffset + getCompactVariableSize(validMembersBitsSize), validMembersOffset + getCompactVariableSize(validMembersBitsSize) + validMembersSize);
            const quorumPublicKeyOffset = validMembersOffset + getCompactVariableSize(validMembersBitsSize) + validMembersSize;
            const quorumPublicKey = bytes.slice(quorumPublicKeyOffset, quorumPublicKeyOffset + 48);
            const quorumVvecHash = bytes.slice(quorumPublicKeyOffset + 48, quorumPublicKeyOffset + 48 + 32);
            const quorumSig = bytes.slice(quorumPublicKeyOffset + 48 + 32, quorumPublicKeyOffset + 48 + 32 + 96);
            const sig = bytes.slice(quorumPublicKeyOffset + 48 + 32 + 96, quorumPublicKeyOffset + 48 + 32 + 96 + 96);
            return new QfCommit(version, llmqType, bytesToHex(quorumHash.toReversed()), bytesToHex(signers), bytesToHex(validMembers), bytesToHex(quorumPublicKey), bytesToHex(quorumVvecHash.toReversed()), bytesToHex(quorumSig), bytesToHex(sig), quorumIndex);
        }
        static fromHex(hex) {
            return QfCommit.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const llmqTypeBytes = new Uint8Array(1);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(llmqTypeBytes.buffer).setUint8(0, this.llmqType);
            const quorumHashBytes = hexToBytes(this.quorumHash).toReversed();
            let quorumIndexBytes = new Uint8Array(0);
            if (this.version >= 2) {
                quorumIndexBytes = new Uint8Array(2);
                new DataView(quorumIndexBytes.buffer).setUint16(0, this.quorumIndex ?? 0, true);
            }
            const signersBytes = hexToBytes(this.signers);
            const signersBitsSizeBytes = encodeCompactSize(signersBytes.byteLength * 8 - 7);
            const validMembersBytes = hexToBytes(this.validMembers);
            const validMembersBitsSizeBytes = encodeCompactSize(validMembersBytes.byteLength * 8 - 7);
            const quorumPublicKeyBytes = hexToBytes(this.quorumPublicKey);
            const quorumVvecHashBytes = hexToBytes(this.quorumVvecHash).toReversed();
            const quorumSigBytes = hexToBytes(this.quorumSig);
            const sigBytes = hexToBytes(this.sig);
            const signersOffset = versionBytes.byteLength + llmqTypeBytes.byteLength + quorumHashBytes.byteLength + quorumIndexBytes.byteLength;
            const quorumPublicKeyOffset = signersOffset + signersBitsSizeBytes.byteLength + signersBytes.byteLength + validMembersBitsSizeBytes.byteLength + validMembersBytes.byteLength;
            const out = new Uint8Array(307 + signersBitsSizeBytes.byteLength + signersBytes.byteLength + validMembersBitsSizeBytes.byteLength + validMembersBytes.byteLength);
            out.set(versionBytes, 0);
            out.set(llmqTypeBytes, versionBytes.byteLength);
            out.set(quorumHashBytes, versionBytes.byteLength + llmqTypeBytes.byteLength);
            out.set(quorumIndexBytes, versionBytes.byteLength + llmqTypeBytes.byteLength + quorumHashBytes.byteLength);
            out.set(signersBitsSizeBytes, signersOffset);
            out.set(signersBytes, signersOffset + signersBitsSizeBytes.byteLength);
            out.set(validMembersBitsSizeBytes, signersOffset + signersBitsSizeBytes.byteLength + signersBytes.byteLength);
            out.set(validMembersBytes, signersOffset + signersBitsSizeBytes.byteLength + signersBytes.byteLength + validMembersBitsSizeBytes.byteLength);
            out.set(quorumPublicKeyBytes, quorumPublicKeyOffset);
            out.set(quorumVvecHashBytes, quorumPublicKeyOffset + quorumPublicKeyBytes.byteLength);
            out.set(quorumSigBytes, quorumPublicKeyOffset + quorumPublicKeyBytes.byteLength + quorumVvecHashBytes.byteLength);
            out.set(sigBytes, quorumPublicKeyOffset + quorumPublicKeyBytes.byteLength + quorumVvecHashBytes.byteLength + quorumSigBytes.byteLength);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                version: this.version,
                llmqType: this.llmqType,
                validMembers: this.validMembers,
                quorumHash: this.quorumHash,
                quorumPublicKey: this.quorumPublicKey,
                quorumSig: this.quorumSig,
                quorumVvecHash: this.quorumVvecHash,
                sig: this.sig,
                signers: this.signers,
                quorumIndex: this.quorumIndex ?? null
            };
        }
    }

    class QcTx {
        version;
        height;
        commitment;
        constructor(version, height, commitment) {
            this.version = version;
            this.height = height;
            this.commitment = commitment;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint16(0, true);
            const height = dataView.getUint32(2, true);
            const commitment = QfCommit.fromBytes(bytes.slice(6));
            return new QcTx(version, height, commitment);
        }
        static fromHex(hex) {
            return QcTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionBytes = new Uint8Array(2);
            const heightBytes = new Uint8Array(4);
            new DataView(versionBytes.buffer).setUint16(0, this.version, true);
            new DataView(heightBytes.buffer).setUint32(0, this.height, true);
            const commitmentBytes = this.commitment.bytes();
            const out = new Uint8Array(6 + commitmentBytes.byteLength);
            out.set(versionBytes, 0);
            out.set(heightBytes, 2);
            out.set(commitmentBytes, 6);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                version: this.version,
                height: this.height,
                commitment: this.commitment.toJSON()
            };
        }
    }

    class MnHfSignal {
        versionBit;
        quorumHash;
        sig;
        constructor(version, quorumHash, sig) {
            this.versionBit = version;
            this.quorumHash = quorumHash;
            this.sig = sig;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint8(0);
            const quorumHash = bytes.slice(1, 33);
            const sig = bytes.slice(33);
            return new MnHfSignal(version, bytesToHex(quorumHash.toReversed()), bytesToHex(sig));
        }
        static fromHex(hex) {
            return MnHfSignal.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const out = new Uint8Array(129);
            const versionByte = new Uint8Array(1);
            new DataView(versionByte.buffer).setUint8(0, this.versionBit);
            out.set(versionByte, 0);
            out.set(hexToBytes(this.quorumHash).toReversed(), 1);
            out.set(hexToBytes(this.sig), 33);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                versionBit: this.versionBit,
                quorumHash: this.quorumHash,
                sig: this.sig
            };
        }
    }

    class MnHfTx {
        version;
        commitment;
        constructor(version, commitment) {
            this.version = version;
            this.commitment = commitment;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint8(0);
            const commitment = MnHfSignal.fromBytes(bytes.slice(1));
            return new MnHfTx(version, commitment);
        }
        static fromHex(hex) {
            return MnHfTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionByte = new Uint8Array(1);
            new DataView(versionByte.buffer).setUint8(0, this.version);
            const commitmentBytes = this.commitment.bytes();
            const out = new Uint8Array(1 + commitmentBytes.byteLength);
            out.set(versionByte, 0);
            out.set(commitmentBytes, 1);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                version: this.version,
                commitment: this.commitment.toJSON()
            };
        }
    }

    class AssetLockTx {
        version;
        count;
        outputs;
        constructor(version, count, outputs) {
            this.version = version;
            this.count = count;
            this.outputs = outputs;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint8(0);
            const count = dataView.getUint8(1);
            const outputs = [];
            let outputPadding = 2;
            for (let i = 0; i < count; i++) {
                const output = Output.fromBytes(bytes.slice(outputPadding));
                outputs.push(output);
                outputPadding += output.bytes().byteLength;
            }
            return new AssetLockTx(version, count, outputs);
        }
        static fromHex(hex) {
            return AssetLockTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionByte = new Uint8Array(1);
            const countByte = new Uint8Array(1);
            new DataView(versionByte.buffer).setUint8(0, this.version);
            new DataView(countByte.buffer).setUint8(0, this.count);
            const outputsBytes = this.outputs.reduce((acc, curr) => {
                const outputBytes = curr.bytes();
                const out = new Uint8Array(outputBytes.byteLength + acc.byteLength);
                out.set(acc, 0);
                out.set(outputBytes, acc.byteLength);
                return out;
            }, new Uint8Array(0));
            const out = new Uint8Array(2 + outputsBytes.byteLength);
            out.set(versionByte, 0);
            out.set(countByte, 1);
            out.set(outputsBytes, 2);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                version: this.version,
                count: this.count,
                outputs: this.outputs.map(output => output.toJSON())
            };
        }
    }

    class AssetUnlockTx {
        version;
        index;
        fee;
        requestedHeight;
        quorumHash;
        quorumSig;
        constructor(version, index, fee, requestedHeight, quorumHash, quorumSig) {
            this.version = version;
            this.index = index;
            this.fee = fee;
            this.requestedHeight = requestedHeight;
            this.quorumHash = quorumHash;
            this.quorumSig = quorumSig;
        }
        static fromBytes(bytes) {
            const dataView = new DataView(bytes.buffer);
            const version = dataView.getUint8(0);
            const index = dataView.getBigUint64(1, true);
            const fee = dataView.getUint32(9, true);
            const requestedHeight = dataView.getUint32(13, true);
            const quorumHash = bytes.slice(17, 49);
            const quorumSig = bytes.slice(49);
            return new AssetUnlockTx(version, index, fee, requestedHeight, bytesToHex(quorumHash.toReversed()), bytesToHex(quorumSig));
        }
        static fromHex(hex) {
            return AssetUnlockTx.fromBytes(hexToBytes(hex));
        }
        bytes() {
            const versionByte = new Uint8Array(1);
            const indexBytes = new Uint8Array(8);
            const feeBytes = new Uint8Array(4);
            const requestedHeightBytes = new Uint8Array(4);
            new DataView(versionByte.buffer).setUint8(0, this.version);
            new DataView(indexBytes.buffer).setBigUint64(0, this.index, true);
            new DataView(feeBytes.buffer).setUint32(0, this.fee, true);
            new DataView(requestedHeightBytes.buffer).setUint32(0, this.requestedHeight, true);
            const quorumHashBytes = hexToBytes(this.quorumHash).toReversed();
            const quorumSigBytes = hexToBytes(this.quorumSig);
            const out = new Uint8Array(145);
            out.set(versionByte, 0);
            out.set(indexBytes, 1);
            out.set(feeBytes, 9);
            out.set(requestedHeightBytes, 13);
            out.set(quorumHashBytes, 17);
            out.set(quorumSigBytes, 49);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        toJSON() {
            return {
                version: this.version,
                index: this.index.toString(),
                fee: this.fee,
                requestedHeight: this.requestedHeight,
                quorumHash: this.quorumHash,
                quorumSig: this.quorumSig
            };
        }
    }

    class Transaction {
        version;
        type;
        #nLockTime;
        inputs;
        outputs;
        extraPayload;
        constructor(inputs, outputs, nLockTime, version, type, extraPayload) {
            this.version = version ?? TRANSACTION_VERSION;
            this.type = type ?? 0 /* TransactionType.TRANSACTION_NORMAL */;
            this.#nLockTime = nLockTime ?? DEFAULT_NLOCK_TIME;
            this.inputs = inputs ?? [];
            this.outputs = outputs ?? [];
            this.extraPayload = extraPayload;
        }
        get nLockTime() {
            if (this.#nLockTime < NLOCK_TIME_BLOCK_BASED_LIMIT) {
                return this.#nLockTime;
            }
            else {
                return !isNaN(this.#nLockTime) ? new Date(this.#nLockTime) : this.#nLockTime;
            }
        }
        set nLockTime(nLockTime) {
            if (nLockTime instanceof Date) {
                this.#nLockTime = nLockTime.getTime();
                return;
            }
            // check for js
            if (typeof nLockTime !== 'number') {
                throw new Error('Invalid nLock time');
            }
            if (nLockTime < 0) {
                throw new Error('nLockTime must be greater than 0');
            }
            this.#nLockTime = nLockTime;
        }
        addInput(inputs) {
            this.inputs.push(inputs);
        }
        addOutput(output) {
            this.outputs.push(output);
        }
        getOutputAmount() {
            return this.outputs.reduce((acc, curr) => {
                return acc + curr.satoshis;
            }, BigInt(0));
        }
        hash() {
            return bytesToHex(doubleSHA256(this.bytes()).toReversed());
        }
        getExtraPayloadType() {
            switch (this.type) {
                case 1 /* TransactionType.TRANSACTION_PROVIDER_REGISTER */:
                    return 'ProRegTx';
                case 2 /* TransactionType.TRANSACTION_PROVIDER_UPDATE_SERVICE */:
                    return 'ProUpServTx';
                case 3 /* TransactionType.TRANSACTION_PROVIDER_UPDATE_REGISTRAR */:
                    return 'ProUpRegTx';
                case 4 /* TransactionType.TRANSACTION_PROVIDER_UPDATE_REVOKE */:
                    return 'ProUpRevTx';
                case 5 /* TransactionType.TRANSACTION_COINBASE */:
                    return 'CbTx';
                case 6 /* TransactionType.TRANSACTION_QUORUM_COMMITMENT */:
                    return 'QcTx';
                case 7 /* TransactionType.TRANSACTION_MASTERNODE_HARD_FORK_SIGNAL */:
                    return 'MnHfTx';
                case 8 /* TransactionType.TRANSACTION_ASSET_LOCK */:
                    return 'AssetLockTx';
                case 9 /* TransactionType.TRANSACTION_ASSET_UNLOCK */:
                    return 'AssetUnlockTx';
            }
            return undefined;
        }
        #signInput(privateKey, inputIndex) {
            if (this.inputs.length < inputIndex) {
                throw new Error(`input with not found (index: ${inputIndex})`);
            }
            if (inputIndex < 0) {
                throw new Error('inputIndex must be greater than 0');
            }
            const publicKey = privateKey.getPublicKey();
            // clone inputs
            const savedInputs = this.inputs.map(input => Input.fromBytes(input.bytes()));
            for (let i = 0; i < this.inputs.length; i++) {
                if (i !== inputIndex) {
                    this.inputs[i].scriptSig = new Script();
                }
            }
            const txBytes = this.bytes();
            const sighashBytes = new Uint8Array(txBytes.length + 4);
            sighashBytes.set(txBytes, 0);
            // push value via link to sighashBytes
            const sighashView = new DataView(sighashBytes.buffer, txBytes.length, 4);
            sighashView.setUint32(0, SIGHASH_ALL, true);
            const derSignature = secp256k1_js.secp256k1.sign(doubleSHA256(sighashBytes), privateKey.key, {
                format: 'der',
                prehash: false,
                lowS: true
            });
            const signatureWithSighash = new Uint8Array(derSignature.length + 1);
            signatureWithSighash.set(derSignature, 0);
            signatureWithSighash[derSignature.length] = SIGHASH_ALL;
            const inputScript = new Script();
            let sigOpcode;
            if (signatureWithSighash.byteLength === 71) {
                sigOpcode = 'OP_PUSHBYTES_71';
            }
            else if (signatureWithSighash.byteLength === 72) {
                sigOpcode = 'OP_PUSHBYTES_72';
            }
            else if (signatureWithSighash.byteLength === 73) {
                sigOpcode = 'OP_PUSHBYTES_73';
            }
            else {
                throw new Error('Invalid signature size');
            }
            inputScript.pushOpCode(sigOpcode, signatureWithSighash);
            inputScript.pushOpCode('OP_PUSHBYTES_33', publicKey.inner);
            this.inputs = savedInputs;
            this.inputs[inputIndex].scriptSig = inputScript;
        }
        // TODO: MultiSig
        sign(privateKey) {
            for (let i = 0; i < this.inputs.length; i++) {
                this.#signInput(privateKey, i);
            }
        }
        generateChange(address, inputAmount) {
            if (this.inputs.length === 0) {
                throw new Error('Before call generateChange you must set all inputs');
            }
            if (this.outputs.length === 0) {
                throw new Error('Before call generateChange you must set all outputs');
            }
            const signedInputsSize = this.inputs.reduce((acc) => acc + BigInt(SIGNED_INPUT_MAX_SIZE), BigInt(0));
            const outputAmount = this.outputs.reduce((acc, curr) => acc + curr.satoshis, 0n);
            if (inputAmount < outputAmount) {
                throw new Error('Inputs amount lower than output amount');
            }
            const dummyTx = Transaction.fromBytes(this.bytes());
            dummyTx.inputs = [];
            const txBytesLength = BigInt(dummyTx.bytes().byteLength) + signedInputsSize;
            const outputsWithChangePrediction = outputAmount + BigInt(CHANGE_OUTPUT_MAX_SIZE) + txBytesLength;
            let changeAmount = inputAmount - outputsWithChangePrediction * BigInt(FEE_PER_BYTE);
            if (inputAmount === outputAmount + changeAmount) {
                return;
            }
            const outputScript = new Script();
            outputScript.pushOpCode('OP_DUP');
            outputScript.pushOpCode('OP_HASH160');
            outputScript.pushOpCode('OP_PUSHBYTES_20', addressToPublicKeyHash(address));
            outputScript.pushOpCode('OP_EQUALVERIFY');
            outputScript.pushOpCode('OP_CHECKSIG');
            if (changeAmount < MIN_FEE_RELAY) {
                changeAmount = BigInt(MIN_FEE_RELAY);
            }
            const changeOutput = new Output(changeAmount, outputScript);
            if (inputAmount < outputAmount + BigInt(this.bytes().byteLength + changeOutput.bytes().byteLength + getCompactVariableSize(this.outputs.length + 1))) {
                return;
            }
            this.outputs.push(changeOutput);
        }
        bytes() {
            // 4 bytes version & type packed
            // varint input count
            // inputs
            // varint output count
            // outputs
            // nLockTime
            const versionWithTypeView = new DataView(new ArrayBuffer(4));
            versionWithTypeView.setInt32(0, this.version | (this.type << 16), true);
            const inputCount = encodeCompactSize(this.inputs.length);
            const inputs = this.inputs.map(input => input.bytes());
            const outputCount = encodeCompactSize(this.outputs.length);
            const outputs = this.outputs.map(output => output.bytes());
            const inputsSize = inputs.reduce((acc, curr) => acc + curr.byteLength, 0);
            const outputsSize = outputs.reduce((acc, curr) => acc + curr.length, 0);
            const inputsBytes = inputs
                .reduce((acc, curr, currentIndex) => {
                const previousBytesSize = inputs.slice(0, currentIndex).reduce((acc, curr) => acc + curr.byteLength, 0);
                acc.set(curr, previousBytesSize);
                return acc;
            }, new Uint8Array(inputsSize));
            const outputsBytes = outputs
                .reduce((acc, curr, currentIndex) => {
                const previousBytesSize = outputs.slice(0, currentIndex).reduce((acc, curr) => acc + curr.byteLength, 0);
                acc.set(curr, previousBytesSize);
                return acc;
            }, new Uint8Array(outputsSize));
            const lockTimeView = new DataView(new ArrayBuffer(4));
            lockTimeView.setUint32(0, this.#nLockTime, true);
            const extraPayloadBytes = this.extraPayload?.bytes() ?? new Uint8Array(0);
            const extraPayloadSizeBytes = encodeCompactSize(extraPayloadBytes.byteLength);
            const out = new Uint8Array(versionWithTypeView.byteLength + inputCount.byteLength + inputsSize + outputCount.byteLength + outputsSize + lockTimeView.byteLength + extraPayloadSizeBytes.byteLength + extraPayloadBytes.byteLength);
            out.set(new Uint8Array(versionWithTypeView.buffer), 0);
            out.set(inputCount, 4);
            out.set(inputsBytes, 4 + inputCount.byteLength);
            out.set(outputCount, 4 + inputCount.byteLength + inputsSize);
            out.set(outputsBytes, 4 + inputCount.byteLength + inputsSize + outputCount.byteLength);
            out.set(new Uint8Array(lockTimeView.buffer), 4 + inputCount.byteLength + inputsSize + outputCount.byteLength + outputsSize);
            out.set(extraPayloadSizeBytes, 4 + inputCount.byteLength + inputsSize + outputCount.byteLength + outputsSize + lockTimeView.byteLength);
            out.set(extraPayloadBytes, 4 + inputCount.byteLength + inputsSize + outputCount.byteLength + outputsSize + lockTimeView.byteLength + extraPayloadSizeBytes.byteLength);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        static fromBytes(bytes) {
            // 4 bytes version & type packed
            // varint input count
            // inputs
            // varint output count
            // outputs
            // nLockTime
            const dataView = new DataView(bytes.buffer);
            const versionWithType = dataView.getInt32(0, true);
            const version = versionWithType & 0xffff;
            const type = (versionWithType >> 16) & 0xffff;
            const inputCount = decodeCompactSize(4, bytes);
            const inputCountSize = getCompactVariableSize(inputCount);
            const inputsPadding = 4 + inputCountSize;
            const inputs = [];
            for (let i = BigInt(0); i < inputCount; i++) {
                const offset = inputs.reduce((acc, curr) => acc + curr.bytes().byteLength, 0);
                const input = Input.fromBytes(bytes.slice(inputsPadding + offset));
                inputs.push(input);
            }
            const outputCountPadding = inputs.reduce((acc, curr) => acc + curr.bytes().byteLength, inputsPadding);
            const outputCount = decodeCompactSize(outputCountPadding, bytes);
            const outputCountSize = getCompactVariableSize(outputCount);
            const outputsPadding = outputCountPadding + outputCountSize;
            const outputs = [];
            for (let i = BigInt(0); i < outputCount; i++) {
                const offset = outputs.reduce((acc, curr) => acc + curr.bytes().byteLength, 0);
                const output = Output.fromBytes(bytes.slice(outputsPadding + offset));
                outputs.push(output);
            }
            const lockTimePadding = outputCountPadding + outputCountSize + outputs.reduce((acc, curr) => acc + curr.bytes().byteLength, 0);
            const nLockTime = dataView.getUint32(lockTimePadding, true);
            let extraPayload;
            if (lockTimePadding + 4 < bytes.length) {
                const extraPayloadSize = decodeCompactSize(lockTimePadding + 4, bytes);
                let extraPayloadHandler;
                switch (type) {
                    case 1 /* TransactionType.TRANSACTION_PROVIDER_REGISTER */:
                        extraPayloadHandler = ProRegTX.fromBytes;
                        break;
                    case 2 /* TransactionType.TRANSACTION_PROVIDER_UPDATE_SERVICE */:
                        extraPayloadHandler = ProUpServTx.fromBytes;
                        break;
                    case 3 /* TransactionType.TRANSACTION_PROVIDER_UPDATE_REGISTRAR */:
                        extraPayloadHandler = ProUpRegTx.fromBytes;
                        break;
                    case 4 /* TransactionType.TRANSACTION_PROVIDER_UPDATE_REVOKE */:
                        extraPayloadHandler = ProUpRevTx.fromBytes;
                        break;
                    case 5 /* TransactionType.TRANSACTION_COINBASE */:
                        extraPayloadHandler = CbTx.fromBytes;
                        break;
                    case 6 /* TransactionType.TRANSACTION_QUORUM_COMMITMENT */:
                        extraPayloadHandler = QcTx.fromBytes;
                        break;
                    case 7 /* TransactionType.TRANSACTION_MASTERNODE_HARD_FORK_SIGNAL */:
                        extraPayloadHandler = MnHfTx.fromBytes;
                        break;
                    case 8 /* TransactionType.TRANSACTION_ASSET_LOCK */:
                        extraPayloadHandler = AssetLockTx.fromBytes;
                        break;
                    case 9 /* TransactionType.TRANSACTION_ASSET_UNLOCK */:
                        extraPayloadHandler = AssetUnlockTx.fromBytes;
                        break;
                    default:
                        throw new Error(`Unsupported extra payload type ${type}`);
                }
                extraPayload = extraPayloadHandler(bytes.slice(lockTimePadding + 4 + getCompactVariableSize(extraPayloadSize), lockTimePadding + 4 + getCompactVariableSize(extraPayloadSize) + Number(extraPayloadSize)));
            }
            return new Transaction(inputs, outputs, nLockTime, version, type, extraPayload);
        }
        static fromHex(hex) {
            return Transaction.fromBytes(hexToBytes(hex));
        }
        toJSON() {
            return {
                version: this.version,
                type: this.type,
                nLockTime: this.#nLockTime,
                outputs: this.outputs.map(output => output.toJSON()),
                inputs: this.inputs.map(input => input.toJSON()),
                extraPayload: this.extraPayload?.toJSON() ?? null
            };
        }
    }

    class InstantLock {
        version;
        inputs;
        txId;
        cycleHash;
        signature;
        constructor(version, inputs, txId, cycleHash, signature) {
            this.version = version;
            this.inputs = inputs;
            this.txId = txId;
            this.cycleHash = cycleHash;
            this.signature = signature;
        }
        bytes() {
            const inputSizeBytes = encodeCompactSize(this.inputs.length);
            const inputsBytes = this.inputs.reduce((acc, curr) => {
                const out = new Uint8Array(acc.byteLength + 36);
                out.set(acc, 0);
                out.set(curr.bytes(), acc.byteLength);
                return out;
            }, new Uint8Array(0));
            const txIdBytes = hexToBytes(this.txId).toReversed();
            const cycleHashBytes = hexToBytes(this.cycleHash).toReversed();
            const signatureBytes = hexToBytes(this.signature);
            const out = new Uint8Array(1 + inputSizeBytes.byteLength + inputsBytes.byteLength + txIdBytes.byteLength + cycleHashBytes.byteLength + signatureBytes.byteLength);
            out.set(new Uint8Array([this.version]), 0);
            out.set(inputSizeBytes, 1);
            out.set(inputsBytes, 1 + inputSizeBytes.byteLength);
            out.set(txIdBytes, 1 + inputSizeBytes.byteLength + inputsBytes.byteLength);
            out.set(cycleHashBytes, 1 + inputSizeBytes.byteLength + inputsBytes.byteLength + txIdBytes.byteLength);
            out.set(signatureBytes, 1 + inputSizeBytes.byteLength + inputsBytes.byteLength + txIdBytes.byteLength + cycleHashBytes.byteLength);
            return out;
        }
        hex() {
            return bytesToHex(this.bytes());
        }
        static fromBytes(bytes) {
            const [version] = bytes;
            const inputSize = decodeCompactSize(1, bytes);
            const inputsBytes = bytes.slice(1 + getCompactVariableSize(inputSize), 1 + getCompactVariableSize(inputSize) + Number(inputSize) * 36);
            const txIdOffset = 1 + getCompactVariableSize(inputSize) + Number(inputSize) * 36;
            const txId = bytes.slice(txIdOffset, txIdOffset + 32);
            const cycleHash = bytes.slice(txIdOffset + 32, txIdOffset + 32 + 32);
            const sig = bytes.slice(txIdOffset + 32 + 32, txIdOffset + 32 + 32 + 96);
            const inputs = [];
            for (let i = 0; i < inputSize; i++) {
                inputs.push(OutPoint.fromBytes(inputsBytes.slice(i * 36, (i + 1) * 36)));
            }
            return new InstantLock(version, inputs, bytesToHex(txId.toReversed()), bytesToHex(cycleHash.toReversed()), bytesToHex(sig));
        }
        static fromHex(hex) {
            return InstantLock.fromBytes(hexToBytes(hex));
        }
        toJSON() {
            const inputsJSON = this.inputs.map(input => input.toJSON());
            return {
                version: this.version,
                inputs: inputsJSON,
                txId: this.txId,
                cycleHash: this.cycleHash,
                signature: this.signature
            };
        }
    }

    class DashCoreSDK {
        grpcConnectionPool;
        constructor() {
            this.grpcConnectionPool = new GRPCConnectionPool('testnet', { dapiUrl: 'http://127.0.0.1:1443', poolLimit: 5 });
        }
        async generateAddress() {
            const { secretKey, publicKey } = secp__namespace.keygen();
            const P2PKH = btcSigner.p2pkh(publicKey, DASH_VERSIONS.testnet);
            return {
                wif: PrivateKey.fromBytes(secretKey, Network.Testnet).toWIF(),
                address: P2PKH.address
            };
        }
        async getTransaction(txid) {
            const client = this.grpcConnectionPool.getClient();
            return (await client.getTransaction(GetTransactionRequest.fromJson({ id: txid }))).response;
        }
        async getBlockchainStatus() {
            const client = this.grpcConnectionPool.getClient();
            return (await client.getBlockchainStatus(GetBlockchainStatusRequest.fromJson({}))).response;
        }
        async getMasternodeStatus() {
            const client = this.grpcConnectionPool.getClient();
            return (await client.getMasternodeStatus(GetMasternodeStatusRequest.fromJson({}))).response;
        }
        async getBlock({ hash, height }) {
            if (hash == null && height == null) {
                throw new Error('Hash or height cannot be undefined or null');
            }
            const client = this.grpcConnectionPool.getClient();
            const req = {};
            if (hash != null) {
                req.hash = hash;
            }
            else {
                req.height = height;
            }
            return (await client.getBlock(GetBlockRequest.fromJson({ ...req }))).response;
        }
        async getBestBlockHeight() {
            const client = this.grpcConnectionPool.getClient();
            return (await client.getBestBlockHeight(GetBestBlockHeightRequest.fromJson({}))).response;
        }
        async broadcastTransaction(transaction, allowHighFees, bypassLimits) {
            const client = this.grpcConnectionPool.getClient();
            return (await client.broadcastTransaction(BroadcastTransactionRequest.create({
                transaction,
                allowHighFees: allowHighFees ?? false,
                bypassLimits: bypassLimits ?? false
            }))).response;
        }
        async getEstimatedTransactionFee(blocks) {
            const client = this.grpcConnectionPool.getClient();
            return (await client.getEstimatedTransactionFee(GetEstimatedTransactionFeeRequest.fromJson({ blocks }))).response;
        }
        subscribeToBlockHeadersWithChainLocks(count = 0, fromBlockHash, fromBlockHeight) {
            throw new Error('Not implemented');
            // const client = this.grpcConnectionPool.getClient();
            //
            // const request = BlockHeadersWithChainLocksRequest.fromPartial({
            //     fromBlockHash: fromBlockHash ? (fromBlockHash) : undefined,
            //     fromBlockHeight,
            //     count,
            // })
            //
            // const stream = client.subscribeToBlockHeadersWithChainLocks(request)
            //
            // return stream
        }
        async waitForPayment(address, amount = 1000) {
            const pendingTransactions = [];
            const iterator = this.subscribeToTransactions([address]);
            for await (const event of iterator) {
                switch (event.event) {
                    case 'rawMerkleBlock': {
                        await wait(5000);
                        for (const pendingTransaction of pendingTransactions) {
                            const dapiTransaction = await this.getTransaction(pendingTransaction.hash());
                            const transaction = Transaction.fromBytes(dapiTransaction.transaction);
                            if (dapiTransaction.isChainLocked && pendingTransaction.hash === transaction.hash && pendingTransaction.outputs
                                .some(output => output.satoshis >= amount &&
                                // @ts-expect-error
                                output.script.toAddress('testnet').toString() === address)) {
                                return {
                                    txid: transaction.hash(),
                                    chainLocked: dapiTransaction.height
                                };
                            }
                        }
                        break;
                    }
                    case 'rawTransaction': {
                        const tx = event.data;
                        if (!pendingTransactions.some(tx => tx.hash)) {
                            const transaction = Transaction.fromBytes(hexToBytes(tx));
                            pendingTransactions.push(transaction);
                        }
                        break;
                    }
                    case 'instantSendLockMessage': {
                        for (const pendingTransaction of pendingTransactions) {
                            const instantSendLock = InstantLock.fromHex(event.data);
                            if (instantSendLock.txId === pendingTransaction.hash() &&
                                pendingTransaction.outputs
                                    .some(output => output.satoshis >= amount &&
                                    // @ts-expect-error
                                    output.script.toAddress('testnet').toString() === address)) {
                                return {
                                    txid: instantSendLock.txId,
                                    instantLocked: event.data
                                };
                            }
                        }
                        break;
                    }
                    default:
                        throw new Error('Unknown message send back from from DAPI (subscribeToTransactionsWithProofs)');
                }
            }
            throw new Error('Unreachable code block in waitForPayment');
        }
        async *subscribeToTransactions(addresses) {
            const numberOfElements = 1;
            const bf = BloomFilter$1.create(numberOfElements, BLOOM_FILTER_FALSE_POSITIVE_RATE);
            bf.insert(base.base58.decode(addresses[0]).slice(1, 21));
            // subscribe to new transactions
            const count = 0;
            // send transaction hashes
            const sendTransactionHashes = false;
            const fromBlockHeight = (await this.getBestBlockHeight()).height;
            const abortController = new AbortController();
            const iterator = this.subscribeToTransactionsWithProofs({ ...bf.toObject() }, count, sendTransactionHashes, undefined, fromBlockHeight, abortController);
            setTimeout(() => {
                abortController.abort('DAPI_RECONNECT_STREAM');
            }, DAPI_STREAM_RECONNECT_TIMEOUT);
            try {
                for await (const event of iterator.responses) {
                    const { responses } = event;
                    switch (responses.oneofKind) {
                        case 'rawMerkleBlock': {
                            yield ({
                                event: 'rawMerkleBlock',
                                data: bytesToHex(responses.rawMerkleBlock)
                            });
                            break;
                        }
                        case 'rawTransactions': {
                            for (const transaction of responses.rawTransactions.transactions) {
                                const tx = Transaction.fromBytes(transaction);
                                if (tx.outputs.some((output) => output.getAddress(Network.Testnet))) {
                                    yield ({
                                        event: 'rawTransaction',
                                        data: bytesToHex(transaction)
                                    });
                                }
                            }
                            break;
                        }
                        case 'instantSendLockMessages': {
                            for (const instantSendLockMessage of responses.instantSendLockMessages.messages) {
                                yield ({
                                    event: 'instantSendLockMessage',
                                    data: bytesToHex(instantSendLockMessage)
                                });
                            }
                            break;
                        }
                        default:
                            break;
                    }
                }
            }
            catch (e) {
                // resubscribe on DAPI timeout or connection errors
                if (e?.message === 'DAPI_RECONNECT_STREAM') {
                    yield* this.subscribeToTransactions(addresses);
                }
                throw e;
            }
        }
        subscribeToTransactionsWithProofs(bloomFilter, count, sendTransactionHashes, fromBlockHash, fromBlockHeight, abortController) {
            const client = this.grpcConnectionPool.getClient(abortController);
            if ((fromBlockHash == null && fromBlockHeight == null) || (fromBlockHash != null && fromBlockHeight != null)) {
                throw new Error('Either fromBlockHash or fromBlockHeight must be set');
            }
            let fromBlock;
            if (fromBlockHash != null) {
                fromBlock = {
                    oneofKind: 'fromBlockHash',
                    fromBlockHash
                };
            }
            if (fromBlockHeight != null) {
                fromBlock = {
                    oneofKind: 'fromBlockHeight',
                    fromBlockHeight
                };
            }
            const request = TransactionsWithProofsRequest.create({
                bloomFilter: {
                    vData: new Uint8Array(bloomFilter.vData),
                    nHashFuncs: bloomFilter.nHashFuncs,
                    nTweak: bloomFilter.nTweak,
                    nFlags: bloomFilter.nFlags
                },
                fromBlock,
                count,
                sendTransactionHashes
            });
            return client.subscribeToTransactionsWithProofs(request);
        }
        subscribeToMasternodeList() {
            const client = this.grpcConnectionPool.getClient();
            const request = MasternodeListRequest.create({});
            return client.subscribeToMasternodeList(request);
        }
    }

    exports.DashCoreSDK = DashCoreSDK;

}));
